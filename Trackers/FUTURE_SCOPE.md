# DailyForge — Future Scope

**Triage completed: April 13, 2026**
**Updated: April 30, 2026 — #169 through #178 added from S13-T1, T7 ships and recovery story (column rename, polish refactors, body-focus matrix flake, sentinel hygiene, helper-module enhancements)**
**Updated: May 3, 2026 — #180 added from S13-T4 + AMENDMENT-1 (custom focus-area icon set + body-map icon)**
**Updated: May 6, 2026 — #187 through #193 added from S13-T6 rescope (5-phase orchestrator, 3-leg chain, startFromList, yoga adapter, model strictness, previewFocus race, yoga swap fallback)**
**Updated: May 10, 2026 — #199 through #204 added from S14-T3 build + `/review` (savasana/yoga_style engine emission, body-focus orphan audit, dead-code fallback, T6 polish bundle, adapter unit tests)**
**Updated: May 17, 2026 — #253 and #254 added from S15-T4 post-build (smoke idempotence, duplicate `not available from strength_tab` throw text to consolidate with S16-T2 typed-error landing); FS #160 CLOSED at feat `9aa95d6` (suggestion engine extraction shipped)**
**Updated: May 17, 2026 — #255, #256, #257 added from S15-T4 `/review` (fitMainCandidate dead code, MOBILITY_MAIN_STYLES dead constant, Sentry wiring on checkRecencyOverlap DB-catch). All three flagged to bundle with S16-T2 typed-error refactor to amortize engine touch.**

Features categorized for Android v1.0 launch vs future updates.

| Category | Count |
|----------|-------|
| 🚀 Before Android Launch | 45 |
| 🔄 Future Updates | 82 |
| ❌ Cut | 2 |
| ✅ Already Done | 7 |

---

## 📌 Approach 5 Pivot — Apr 26, 2026

A strategic planning session on Apr 26, 2026 redirected the app's home-page model from "5-phase-session-as-flagship" to **plan-first with cross-pillar focus areas**. See `Trackers/PRE_SPRINT_11_PLANNING.md` for the full decisions doc.

**Items in this file affected by the pivot are annotated inline** — look for ⚠️ APPROACH 5 IMPACT notes. Items 120 and 121 (the planning session itself + cardio integration) are now ✅ resolved by the planning session. New items 122–129 capture work surfaced by Approach 5 design decisions.

**Note (post-S11-T1, Apr 26, 2026):** Items #122–#129 are now scheduled across Sprints 11–16 in `SPRINT_TRACKER.md` (Sprint 11 locked; Sprints 12–16 TBD). For now they remain mirrored here. Decision pending on the convention — whether scheduled items move to `SPRINT_TRACKER.md` exclusively as each sprint kicks off, or stay mirrored in this file until shipped.

---

## 🚀 Before Android Launch (48 items)

| # | Feature | Category | Notes |
|---|---------|----------|-------|
| 4 | Push notifications | Engagement | Streak warnings, workout reminders |
| 5 | OAuth login (Google, Apple) | Auth | Sign in with Google/Apple buttons |
| 7 | Automated testing (Playwright) | DevOps | Comprehensive test coverage |
| 10 | Form tip text overlays | UI Enhancement | Contextual tips during exercise |
| 11 | Breathing cue animations | UI Enhancement | Visual breathing indicators synced to phases |
| 12 | Form line/arrow animations | UI Enhancement | Animated lines showing correct alignment |
| 13 | 3D rigged character model | Media | Tripo AI + Blender pipeline for consistent character |
| 14 | Exercise demo videos | Media | 35 complex exercises as MP4 |
| 17 | Exercise swap for yoga poses | Yoga | Replace individual poses mid-session |
| 18 | Breathwork technique swap in session | Breathwork | Change technique mid-session |
| 20 | Ashtanga practice type | Yoga | Add Ashtanga sequences |
| 21 | Chair Yoga practice type | Yoga | Accessibility seated yoga option |
| 27 | Goal setting + milestone badges | Body | Target weight, progress %, badges at 25/50/75/100% |
| 29 | Batch strength suggestions endpoint | DevEx | Performance optimization for API calls |
| 30 | Algorithm Spec Doc implementation | Algorithm | 5-level yoga difficulty system |
| 31 | Weekly routine scheduling | UX | Assign routines to specific days. ⚠️ APPROACH 5 IMPACT: Likely supersedes / merges with #122 (weekly plan UI). Revisit when sprint breakdown is designed. |
| 32 | Most improved exercise metric | Analytics | Dashboard showing biggest gains |
| 33 | Pose prerequisites system | Algorithm | Intermediate poses require beginner completion |
| 34 | User level tracking | Algorithm | ✅ **CLOSED — shipped as S11-T4 (Apr 27, 2026, commit `e61e4b3`).** Per-pillar level tracking schema (`user_pillar_levels` table) + research-grounded inference function (`recompute_user_pillar_level`). Thresholds: strength uses time-on-task floor + bodyweight performance ratio (ExRx, StrengthLevel, Santos-Junior 2021); yoga uses frequency-over-time; breathwork uses technique-tier exposure. Promotion-only; respects `declared` / `manual_override` sources. Backfill ran for all existing users. Foundational input for the Sprint 12 suggestion engine. Spec: `Trackers/S11-T4-level-tracking-spec.md`. |
| 35 | Beginner onboarding quiz | Onboarding | Ask fitness level, goals, equipment on first launch. ⚠️ APPROACH 5 IMPACT: Onboarding quiz expands to also capture initial focus preferences and seed the user's first weekly plan. See PRE_SPRINT_11_PLANNING.md §8 (open items: onboarding flow). |
| 36 | Progressive difficulty unlocking | Algorithm | Unlock harder poses as user progresses |
| 38 | Beginner programs | Content | "First 7 Days" guided programs for all pillars. ⚠️ APPROACH 5 IMPACT: Still valuable, but reframe as "starter weekly plans" the suggestion engine offers as templates. |
| 39 | Volume trend comparisons | Analytics | Week-over-week comparison in charts |
| 40 | Counter-pose engine | Algorithm | Auto-insert counter-poses (backbend → forward fold) |
| 41 | Movement pattern tagging | Data | Tag exercises as push/pull/squat/hinge/carry/core. ⚠️ APPROACH 5 IMPACT: This IS body-focus tagging for the strength pillar. Already partially in place via existing muscle tags; movement-pattern tags are the next layer. Promote priority. |
| 42 | Breathwork contraindications | Data | ✅ **CLOSED — folded into S11-T1 (Apr 26, 2026).** The `contraindications TEXT[]` column already existed on `breathwork_techniques` from a prior sprint; pre-flight inspection during S11-T1 prep confirmed no migration was needed. The column predated this FUTURE_SCOPE entry. Conditions per technique (pregnancy, heart conditions) are populated via the existing seed. |
| 43 | Time-of-day breathwork filtering | Algorithm | Morning=energizing, Evening=calming. ⚠️ APPROACH 5 IMPACT: Derivable from the new `effect` tag (energize / calm / balance / focus) — once breathwork tagging ships, this filtering is essentially free. |
| 44 | User equipment profile | Onboarding | Store user's available equipment for filtering |
| 45 | Primary/secondary muscle split | Data | Distinguish main vs supporting muscles |
| 46 | Exercise library "Do this" button | UX | Browse exercise → Start single-exercise session |
| 49 | Yoga pose explorer | UX | Browse poses → single-pose timed hold |
| 52 | useYogaSwap hook extraction | Refactor | Extract yoga swap logic to reusable hook |
| 53 | Breathwork cancel-resume handling | UX | Handle cancel/resume edge cases |
| 54 | Alternatives caching | Performance | Cache exercise alternatives to reduce API calls |
| 55 | Remove exercise from workout | UX | X button to remove added exercises mid-workout |
| 56 | Workout start optimization | Performance | Faster "Starting workout..." screen |
| 57 | Extract useAddExercise hook | Refactor | Deduplicate add exercise logic |
| 59 | Routine edit/reorder exercises | UX | Edit routines: rename, reorder, add/remove |
| 60 | Routine duplicate/fork | UX | Duplicate routine as starting point |
| 61 | Breathwork audio cues | Breathwork | Phase transition sounds. Options: (A) Simple chimes/tones for inhale/hold/exhale transitions, (B) Ambient soundscapes (ocean, forest, singing bowls), (C) Voice guidance via TTS ("inhale... hold... exhale..."), (D) Premium music + voice like Othership. Start with A or C, add B/D as Pro/Max tier feature. Use `just_audio` or `audioplayers` package. |
| 62 | Premium breathwork soundscapes (Pro/Max) | Breathwork | Music-driven sessions like Othership. Curated soundscapes with background music + voice guidance. Could license royalty-free ambient tracks or commission custom production. Differentiates Pro/Max tiers. Research: Othership charges $17.99/mo primarily for their DJ-curated soundscapes. |
| 74 | Option C: LLM-assisted muscle activation tagging | Data/Algorithm | Tag all 736 exercises with primary + secondary muscles using LLM query + cross-reference with free-exercise-db + web research for disagreements. ~$5 one-time LLM cost. Creates new `exercise_muscles` table with (exercise_id, muscle_group, role: primary/secondary). Level 2 accuracy target (~88-92%). Enables accurate heatmap on 3D body map. Can run in parallel with other sprints. Est 1-2 weeks spread work. Added Apr 20, 2026 |
| 75 | Option D: Manual tagging fallback for default program exercises only | Data/Algorithm | If Option C proves too time-consuming, fallback path: hand-tag only the ~30 exercises in the default weekly program with perfect primary + secondary muscles. Rest of 706 exercises use primary-only from free-exercise-db. Accuracy for default-program users: ~95%. Accuracy for library users: ~70%. Launch-ready pragmatic backup. Added Apr 20, 2026 |
| 107 | api_service.dart ClientException rewrap | DevEx | ✅ **Done Apr 24, 2026, commit `4f244f2` (S10-T5c-b).** `_send` and `getList` in `lib/services/api_service.dart` now catch `http.ClientException` and rewrap as `NetworkException`. Defensive catch in BodyMapProvider no longer needed but left in place (harmless). Graceful airplane-mode error verified on Android. |
| 112 | Muscle-mode selected-muscle card still mock | Feature completion | ✅ **Done Apr 24, 2026, commit `4f244f2` (S10-T5c-b).** Extended muscle-volumes endpoint to return per-muscle detail payload (lastTrained / volumeLabel / topExercise / setsThisWeek). `MuscleDetail.fromJson` wired through BodyMapProvider. Muscle-mode card now reactively updates from real backend data. Device verified on Android. |
| 113 | 5-phase sessions split across pillar bars | Analytics completeness | T5c-b ships with `type IN ('strength', '5phase')` treated as strength in weekly chart and minutes this week. Once Sprint 11 ships the 5-phase orchestrator and users run real 5-phase sessions, revisit: (a) give 5phase its own bucket in the weekly chart (4th color/stack segment), or (b) split proportionally across breath/yoga/strength bars based on phase durations. 2-line change per T5c-b build report. Added Apr 24, 2026. ⚠️ APPROACH 5 IMPACT: Still relevant — the analytics question survives the pivot. The 5-phase session ships eventually, just not as the home page identity. |
| 114 | Add `pillar` column to `workouts` table | Schema hygiene | Currently no clean pillar discriminator on workouts; T5c-b avoided needing one because the home page uses user-initiated pillar choice (not inference). Will matter for Sprint 12+ analytics queries that want to bucket workouts by pillar without scanning exercises. Added Apr 24, 2026. |
| 115 | User-configurable week start day | UX personalization | Home page stats + weekly chart currently use Monday as week start (matches dashboard convention via startOfWeekMonday helper). Add a Settings option for user to choose Sunday / Monday / Saturday as their week start. Applies to both dashboard and home page consistently. Added Apr 24, 2026. ⚠️ APPROACH 5 IMPACT: Becomes more important under Approach 5 — the weekly plan UI will be a primary surface, so week-start preference visibility matters. |
| 116 | Home page UI redesign | UX polish | Current T5c-b home page ships functional but not visually best-in-class. Revisit layout, visual hierarchy, and section ordering for a more distinctive, premium feel. Reference: research done Apr 24, 2026 on top apps (Spotify 2026 adaptive layouts, Calm's daily ritual framing, Nike Training Club's content library). Consider adaptive personalization (reorder pillars by usage), richer pillar card visuals, or a hero "today's practice" module. ⚠️ APPROACH 5 IMPACT: This item is now SUBSUMED by the Approach 5 home page redesign (PRE_SPRINT_11_PLANNING.md §2). Mark as resolved by the new sprint plan rather than an independent item. |
| 117 | lastTrained window beyond 90 days | Feature completeness | Muscle-mode card currently shows "Not yet" for any muscle not trained in the trailing 90 days. Acceptable for v1 but may confuse users who have a long training history. Revisit if feedback indicates. Simple config change. Added Apr 24, 2026. |
| 118 | Custom workout builders per pillar | Feature | 🔁 **SUPERSEDED by S14-T5 (Approach 5 cross-pillar session composer).** Per-pillar builders give way to a single cross-pillar composer — same primitives (strength sets/reps, yoga sequences, breathwork patterns) surfaced inside one composer UX rather than three separate ones. Original framing: User-built routines inside each pillar page (strength / yoga / breathwork). User picks exercises/poses/techniques, saves the routine, starts it. Added Apr 24, 2026; superseded Apr 26, 2026. |
| 119 | Adaptive home page personalization | UX / ML | Beyond the static home page redesign: home page layout adapts based on user's actual usage pattern. If user does yoga every morning, yoga card rises to the top. If user does strength 5x/week and breathwork 1x/month, strength is more prominent. Reference: Spotify 2026 layout personalization, Google Maps context modes. Requires usage telemetry + simple ranking algorithm. Sprint 12+ scope. Added Apr 24, 2026. ⚠️ APPROACH 5 IMPACT: Reframe — under Approach 5, "personalization" is the suggestion engine choosing which session to suggest, not which card to surface. Item still valid but its scope shrinks. |
| 120 | Full session philosophy planning session | Product | ✅ **DONE Apr 26, 2026.** Strategic planning session completed; produced `Trackers/PRE_SPRINT_11_PLANNING.md`. Outcome: pivoted to Approach 5 (plan-first home page with cross-pillar focus areas). Philosophy framing: science-first (concurrent training, HRV, mobility-before-strength). 5-phase session repositioned as one mode among several, not the home page identity. Original 4-question scope superseded by Approach 5 design decisions. |
| 121 | Cardio integration in full session | Product | ✅ **DONE Apr 26, 2026.** Resolved in Approach 5 planning session. Decision: cardio is NOT a 4th pillar, NOT a separate session type, NOT a phase. It's a focus-area trainable through any pillar (strength → kettlebell circuits/AMRAP, yoga → power vinyasa, breathwork → Bhastrika/Wim Hof). Falls out of the focus-area model naturally. No new tables, no new tracking type. See PRE_SPRINT_11_PLANNING.md §5. |
| 122 | Approach 5 — Breathwork tagging schema content work | Data / Approach 5 | ✅ **CLOSED — fully shipped through S11-T1 / S11-T1.5 / S11-T2 (Apr 26-27, 2026).** S11-T1 (commit `a407012`): added 5 nullable columns reusing existing `category` / `difficulty` / `contraindications`. S11-T1.5 (commit `ac6a493`): migrated to per-difficulty model — dropped `duration_min` / `duration_max`, added 6 columns (`beginner_duration_min/max`, `intermediate_duration_min/max`, `advanced_duration_min/max`). S11-T2 (commit `f4cd69c`): applied v3 spec (`Trackers/S11-T2-tagging-spec.md`) populating 9 columns × 49 techniques. Distribution: 20 pre-workout / 29 post-workout / 7 standalone-false; 8 rows have beginner-tier NULL per Decision 3 (Option A schema-enforced safety). Foundational unblocker for the suggestion engine (S11-T3 + Sprint 12) — DONE. |
| 123 | Approach 5 — Focus-area data model (body-focus + state-focus) | Schema / Approach 5 | ✅ **CLOSED — shipped as S11-T3 (Apr 27, 2026, commit `6c12604`).** Schema + seed for the two-track focus model. Tables: `focus_areas` (17 rows: 12 body + 5 state), `focus_muscle_keywords` (35 rows, 10 body focuses tagged — `mobility` and `full_body` are special-cased in S12 service-layer logic per spec), `focus_content_compatibility` (54 rows, all role-tagged, `weight` column nullable and unpopulated — values land in S12 once engine semantics are pinned). Hybrid materialization: body-focus derives from existing `target_muscles` at query time; state-focus is materialized. No API endpoint (S12 scope). Spec: `Trackers/S11-T3-focus-area-spec.md`. Foundational unblocker for the Sprint 12 suggestion engine. |
| 124 | Approach 5 — Suggestion engine | Algorithm / Approach 5 | Given a user's chosen focus + level + history, generate a recommended session. Pulls from existing strength exercises, yoga poses, and breathwork techniques (now with Approach 5 tags). Handles bookend logic (energizing breath at start of body-focus session, calming breath at end) and main-work selection for state-focus sessions. Real algorithm work, not a static template. Likely the longest single ticket of the Approach 5 build. Added Apr 26, 2026. |
| 125 | Approach 5 — Weekly plan UI | Feature / Approach 5 | Calendar view, day-by-day focus picker, edit flows ("copy last week," "swap two days"), default plan templates per archetype. Per Apr 26 planning, weekly planning is one level deeper than the home page (not the home page itself) — home page shows "today's planned session," planner is accessed via a dedicated route. Specifics TBD in dedicated UI planning session. Added Apr 26, 2026. |
| 126 | Approach 5 — Session composer (cross-pillar) | Feature / Approach 5 | Build-your-own session UX. Browse / search across all three pillars (strength + yoga + breathwork) in one view, pick exercises/poses/techniques, see total duration update live, save the day's session. Replaces / merges with item #118 (per-pillar custom workout builders). Hybrid with smart default per Option C — coexists on the home page with the suggested session, both surfaced equally. Added Apr 26, 2026. |
| 127 | Approach 5 — New home page (today's planned session, hybrid suggest+build) | Feature / Approach 5 | Replaces the current T5c-b home page model (three pillar cards + Full Session placeholder). New home page shows today's planned session with two coexisting affordances: (1) accept the suggestion (one-tap Start) or (2) build your own. Three-pillar cards either go away or move to a secondary nav surface. 3D body map role TBD — could remain as a focus-picker shortcut or move to profile/analytics. Stats row + weekly chart likely preserved as "execution telemetry." Added Apr 26, 2026. |
| 128 | Approach 5 — Onboarding flow | Feature / Approach 5 | Onboarding asks user's experience level + initial focus preferences and seeds a starter weekly plan. Avoids "quiz feels like a chore" pattern. Surfaces philosophy without preachiness. Replaces / extends item #35 (beginner onboarding quiz). Specifics TBD in dedicated UI planning session. Added Apr 26, 2026. |
| 129 | Approach 5 — 5-phase session repositioned as one mode | Feature / Approach 5 | The 5-phase session (breath → yoga warmup → strength → yoga cooldown → breath) is preserved but no longer the home page identity. Surfaces as an explicit option when user picks a body-focus and wants the unified version (e.g., "biceps day → Full Practice mode"), as a default suggestion on certain calendar contexts (full-body day), and via a "Practice modes" section. Discoverable, not promoted. Original Sprint 11 T1 scope absorbed into this item, sized down. Added Apr 26, 2026. |
| 130 | Upgrade pg / pg-connection-string before pg v3.0.0 SSL changes | DevOps / Security | Discovered during S11-T1 verification (Apr 26, 2026). Running `node src/db/migrate.js` emits a deprecation warning from pg-connection-string about SSL mode handling in the upcoming pg v3.0.0. Modes 'prefer', 'require', and 'verify-ca' currently behave as aliases for 'verify-full' but will adopt standard libpq semantics (weaker guarantees) in v3.0.0. Action when picked up: (a) audit current connection string in `.env` (which sslmode is in use?), (b) pin pg version explicitly OR migrate to explicit `sslmode=verify-full` OR migrate to `uselibpqcompat=true&sslmode=require`, depending on desired security posture for Neon Postgres connections, (c) test against Neon Singapore region to confirm SSL still negotiates cleanly. Priority: Low. Warning only, no current impact. Resolve before pg v3.0.0 ships (no firm date from upstream as of Apr 2026). Added Apr 26, 2026. |

---

## 🔄 Future Updates (75 items)

| # | Feature | Category | Notes |
|---|---------|----------|-------|
| 1 | Nutrition / macro tracking | Strength | Food database API integration |
| 2 | Social feed | Social | Follow users, like workouts, leaderboards |
| 8 | ExerciseDB API upgrade (Option B) | Data | Replace free-exercise-db base (736 exercises) with ExerciseDB API (11K+ exercises). Includes structured primary + secondary muscle tags. Requires RAPIDAPI_KEY (~$10-25/mo subscription). **DEFERRED TO POST-REVENUE** per CEO decision Apr 20, 2026 — bootstrap with Option C (zero-cost LLM tagging) first; upgrade when app generates income. |
| 9 | Muscle highlight overlay | UI Enhancement | SVG toggle on exercise videos |
| 15 | iOS wake lock | Platform | Needs Capacitor native wrapper |
| 16 | Native haptic feedback | Platform | iOS needs native wrapper |
| 22 | Progress photos (Max tier) | Body | Front/side/back with comparison |
| 23 | Full 14-measurement body tracking | Body | Expand from 5 to 14 measurements |
| 24 | Custom body measurements (Max tier) | Body | User adds custom measurement types |
| 25 | Apple Health / Google Fit sync | Integration | Sync workouts to health platforms |
| 26 | True adaptive AI coaching | AI | ML-based weekly adjustments (needs 10K+ sessions) |
| 28 | ML-powered progressive overload | AI | Replace rule-based with ML (needs data) |
| 37 | Muscle recovery tracking | Algorithm | Per-muscle recovery % (0-100) affects exercise selection. Moved from Before Android Launch (Apr 17, 2026) — now partially addressed by 3D body heatmap (T5a shows volume as recovery proxy); true recovery %s can come later |
| 63 | Expanded chart view (tap to enlarge) | Analytics | Full-screen chart modal with zoom, pan, tap-for-details. Added Apr 17, 2026 |
| 64 | Yoga/Breathwork data layer on 3D body map | UI v2 | After launch, add third toggle "Breath" showing lungs/chest activity + yoga-specific flexibility zones on figure |
| 65 | Per-muscle detail drill-down page | UX v2 | Tap "View ›" on muscle tooltip card → full page with exercise history, PR timeline, recovery status, recommended exercises for that muscle |
| 66 | 3D body muscle raycasting (precise tap detection) | Tech v2 | T5a uses hotspot approach; v2 upgrades to true per-mesh raycasting for pixel-perfect muscle selection |
| 67 | Personalized 3D character (replace generic model) | Media v2 | Use Tripo AI–generated character (existing pipeline) as the body map model, so user sees themselves instead of generic anatomy |
| 68 | Body map time-travel (see past volume) | Analytics v2 | Slider above body to scrub through weeks/months showing how muscle balance evolved |
| 69 | Muscle balance score + recommendations | Algorithm v2 | Detect imbalances (e.g., chest trained 3x more than back) and suggest corrective exercises |
| 70 | True per-mesh raycasting via engine swap | Tech v2 | Currently blocked by `model_viewer_plus` WebView architecture. Requires switching to `flutter_scene` or custom platform channels. Weeks of engineering work. Only pursue if v1 performance/precision proves inadequate post-launch. Added Apr 17, 2026 |
| 71 | Hotspot to split-mesh upgrade (if hotspots ship as T5a fallback) | Tech v2 | If T5a ships with polished hotspot zones due to Blender blockers, v2 should upgrade to true split-mesh coloring for precision. Conditional future item — only applies if hotspot fallback was taken. **RESOLVED Apr 20, 2026:** Split-mesh approach working successfully; this item is no longer needed since we're not using hotspot fallback. Kept for historical record. |
| 72 | Two-model overlay approach (beautiful + invisible detection) | Tech v2 | Creative idea: layer CharacterZone model for rendering + invisible primitive shapes for tap detection, moving in sync. Not pursued in T5a due to complexity. Could revisit if hotspot precision proves inadequate. Added Apr 17, 2026 |
| 73 | Custom coordinate-based mesh splitting script | Tech v2 | Write Python script that splits mesh by vertex coordinates (chest = vertices where x between A and B, etc.). Alternative to manual Blender splitting if automated approach is preferred. **PARTIALLY IMPLEMENTED Apr 20, 2026:** We ARE using coordinate-bounds Python scripts for splitting. Future work could automate full body in one batch script instead of per-muscle interactive runs. |
| 76 | Refine muscle boundaries with per-vertex painting | Tech v2 | V1 muscle splits are coordinate-box approximations (70-85% accuracy). V2 could use Blender's sculpt-mode vertex painting with anatomy reference for higher precision. Or commission Fiverr 3D artist ($30-50) post-revenue. Added Apr 20, 2026 |
| 77 | Blender MCP integration for Tripo AI character pipeline | Tech v2 | Connect Blender to Claude Code via blender-mcp addon. Useful for future exercise character posing pipeline (Tripo AI character → Blender pose → render). 30-60 min setup cost. Deferred to when 3D character pipeline becomes active priority. Added Apr 20, 2026 |
| 78 | Revisit body map scope if v1 mesh quality disappoints | UX v2 | If user feedback on v1 body map is "boundaries look off," consider scope-down to 6 major regions (upper body front / upper body back / core / arms / upper legs / lower legs) with simpler visual treatment. Alternative to paying for precision refinement. Only pursue if feedback demands it. Added Apr 20, 2026 |
| 79 | Automated full-body split script (one-shot bounds file) | Tech v2 | Current workflow requires clicking body_lo → Tab → Alt+A → paste script → Alt+P → P → Tab → rename → Ctrl+S per muscle. For future models (Tripo AI personalized characters), write a single Python script that takes a JSON of all muscle bounds and splits + renames + saves in one pass. Would turn 30 muscles from 90 min to 5 min. Added Apr 20, 2026. **UPDATE Apr 21, 2026:** Workflow evolved to hybrid approach (script + manual polish) rather than pure one-shot bounds file. Future auto-split script should accept bounds AND allow for per-muscle manual polish step. Estimated effort now higher than original "30 muscles in 5 min" estimate. |
| 80 | Muscle mesh regeneration for new anatomy models | Tech v2 | Current bounds (documented in S10-T5-DESIGN.md) are specific to the CharacterZone model's coordinate system. Different models will have different proportions and scales. When swapping to Tripo AI personalized character (item 67), mesh splits will need to be redone. Consider auto-calibration: detect skeleton, derive landmarks, scale bounds. Added Apr 20, 2026 |
| 81 | Mesh split quality review pass | Tech v2 | After all 30 muscles are split, do a quality review pass in Flutter app running with mock data. See which muscles feel "off" when tapped (wrong coverage, missing faces, bleed into neighbors). Prioritize fixes for the worst 3-5 muscles before launch. Acceptable polish work before going live. Added Apr 20, 2026 |
| 82 | User preference: show/hide muscle splits | UX v2 | Settings toggle: "Show anatomical muscle view" (split/colored) vs "Show uniform figure" (one color, no splits). Some users may prefer the clean look, especially for meditation/breathwork screens. Low priority. Added Apr 20, 2026 |
| 83 | Per-side oblique asymmetry visualization | UX v2/Max | Oblique meshes are split L/R but grouped as single "oblique" in Flutter for v1. Max-tier feature: surface L/R imbalance in training volume to help user identify weak-side development patterns. Added Apr 21, 2026 |
| 84 | Per-side upper_back asymmetry visualization | UX v2/Max | Same pattern as obliques — split in mesh, grouped in code. Max-tier could show trap imbalance from asymmetric compound lifts. Added Apr 21, 2026 |
| 85 | Per-side lat asymmetry visualization | UX v2/Max | Same pattern. Max-tier could help identify pull-side dominance (common in mixed-hand sports). Added Apr 21, 2026 |
| 86 | Circle-select workflow documented in split guide | Docs | Apr 21 learning: coordinate-bounds scripts work for regular-shaped muscles (arms, abs), but irregular muscles (traps, lats, glutes) benefit from manual Circle Select polish. For any future model migrations, document this hybrid workflow. Added Apr 21, 2026 |
| 87 | Mesh quality review pass includes boundary bleed check | Tech v2 | During v1 polish, specifically check lats_R (known to have some stray face patches near armpit/front that weren't cleaned during session — 2-3 min cleanup). Added Apr 21, 2026. **STATUS Apr 22, 2026:** Not yet addressed; still pending review pass after T5a ships and Flutter renders the mesh. If no visible issue in-app, deprioritize. |
| 88 | Automated split verification script | Tech v2 | Write a Python script that verifies all 28 meshes exist with correct names, reports face counts, and flags any orphan/stray faces. Run before handoff to Flutter code. Saves 10 min of manual verification. Added Apr 21, 2026 |
| 89 | Dedicated adductor / abductor mesh (inner + outer thigh) | Anatomy v2 | V1 uses Option A: hamstring absorbs inner and outer thigh wraps. If users give feedback like "adductor training doesn't light anything specific," v2 could split the current ham_L/R meshes further into distinct adductor (inner), abductor (outer), and hamstring (back) regions. Would require re-splitting via Blender + updating mesh-to-DB grouping in Flutter. Estimated effort: half day Blender + 2-3 hours Flutter. Added Apr 22, 2026 |
| 90 | Per-side glute asymmetry visualization | UX v2/Max | Glutes were merged into single mesh in v1 (user doesn't distinguish per-side in fitness context). If Max-tier asymmetry feature ships later, glutes would need to be re-split into glute_L/R. Flag this re-work cost when scoping the asymmetry feature. Added Apr 22, 2026 |
| 91 | Per-side lower_back asymmetry visualization | UX v2/Max | Same pattern as glute. lower_back merged in v1, would need re-split for Max-tier asymmetry. Lumbar asymmetry can indicate rotational imbalance common in golf/tennis athletes. Added Apr 22, 2026 |
| 92 | Knee Z landmark = -0.115 for CharacterZone model | Docs | Critical landmark for any future leg-muscle operations on this model. Discovered Apr 22 during quad_L split iterations. Original estimate of -0.128 was ~10mm off. Added Apr 22, 2026 |
| 93 | Blender mesh re-split pass for visual quality | Tech v2 / Before Launch | **PRIORITY.** Current coordinate-bounds splits produce highlight regions that don't match user expectation (especially bicep, tricep, delt — rounded muscles where box selection missed 50-70% of the perceived surface). Redo these muscles with Circle Select in Blender. Target: whole-muscle coverage so "tap the bicep → the whole bicep lights up." Likely candidates for re-split: bicep_L/R, tricep_L/R, delt_L/R, forearm_L/R, and any others identified during full-body heatmap visual audit. Re-export GLB as drop-in replacement — same filename, same 27 mesh names, no Flutter code changes needed. Estimated effort: half day Blender. **This item gates all S10-T5a UX polish (items #94-#102).** Added Apr 23, 2026. ⚠️ APPROACH 5 IMPACT: Re-split work still valuable IF the 3D body map remains a primary surface in Approach 5. Decision deferred to UI specifics planning session — if body map becomes a profile/analytics surface rather than home, this priority drops. |
| 94 | Full-body heatmap always-visible on home page body map | UX polish | Currently figure renders plain white; heatmap colors only apply when a muscle is selected. Target behavior: heatmap is permanently visible (quads deep coral, glutes gray, etc.) as an at-a-glance recovery/volume overview — Apple Health style. Selection adds a brighter overlay on top of the heatmap base. Fix location: `body_map_3d.dart` patchColors logic. Blocked on #93 because the heatmap will showcase the weakest splits most visibly. Added Apr 23, 2026. ⚠️ APPROACH 5 IMPACT: Same as #93 — depends on whether body map remains on the home page. |
| 95 | Group-level highlighting on tap (both sides light up together) | UX polish | Tap `chest_L` → both `chest_L` AND `chest_R` highlight as a unified "Chest" group. Matches the muscle card's group-level data ("Chest — 14,280 kg"). Implementation: pass `dbGroupToMeshes[selectedGroup]` as `preselectedEntities` on Interactive3d when selection changes. Flexibility mode: same pattern for region → meshes. Keep meshes individually addressable at data layer for future Max-tier asymmetry feature (#96). Blocked on #93 so highlights apply to correctly-shaped meshes. Added Apr 23, 2026. |
| 96 | Max-tier per-side asymmetry screen | Premium v2 | Dedicated "Muscle Balance" screen (not home page). Shows left/right volume ratios as paired bars, flags imbalances >15%, suggests corrective exercises. Example: "Your left lat is 20% weaker than your right — try single-arm rows." Pre-requisites: (a) unilateral exercise tagging in DB, (b) per-side volume tracking in session logging. Uses the individually-split meshes (chest_L/R, bicep_L/R, etc.) that were preserved in the Blender work. Accessible from muscle drill-down (item #65). Max tier only — a major differentiator vs Hevy/Strong for serious lifters. Consolidates and supersedes items #83, #84, #85, #90, #91. Added Apr 23, 2026. |
| 97 | Body map gesture conflict with parent scroll view | UX polish | On the home page, vertical drag on the 3D figure to rotate (especially when dragging toward top of figure) is intercepted by the outer CustomScrollView, causing the whole page to scroll instead of the figure rotating. Fix: wrap Interactive3d in a gesture arena widget (e.g. RawGestureDetector with PanGestureRecognizer) that claims vertical drags within the figure's bounds. Standard Flutter pattern, ~10-15 lines. Blocked on #93 because the figure bounds/sizing may change. Added Apr 23, 2026. |
| 98 | Outfit font not rendering on light-mode home page | UX polish | Claude Code reported during Step 3 that Outfit font falls back to system font on the home page. Either the font isn't in pubspec.yaml's assets despite being "loaded in S7," or the local light-mode Theme override on HomePage isn't inheriting the fontFamily. Fix: audit pubspec.yaml for the font files, confirm they load, pass `fontFamily: 'Outfit'` explicitly in HomePage's Theme wrapper. Not blocked on Blender but deferred as part of the polish pass. Added Apr 23, 2026. |
| 99 | Today's session card Start button wires to real workout | Feature completion | ✅ **RESOLVED BY S13-T2 (Approach 5 home page suggested-session card).** Currently the Start button on TodaySessionCard is non-functional (marked TODO(S10-T5c) in code). Under Approach 5 it wires to the suggested-session card on the new home page — Start runs the planned session for today, which may or may not be a 5-phase session. Original wire-up plan (start the 5-phase session directly) replaced. Added Apr 23, 2026; resolved-by S13-T2 Apr 26, 2026. |
| 100 | "View history ›" button on selected muscle card | Feature completion | Non-functional in T5a by design. Wires to per-muscle drill-down page (item #65). Blocked on #65. Added Apr 23, 2026. |
| 101 | Full visual audit of all 26 muscle splits with heatmap visible | QA | Once item #94 ships (heatmap always visible), do a systematic pass: set mock volumes to distinct values per group, rotate the figure 360° and from all angles, note every muscle where the highlight region looks wrong. Creates the exact re-split list for #93. Could be the trigger that reveals splits beyond the obvious ones (bicep/tricep/delt) also need work. Added Apr 23, 2026. |
| 102 | Remove spike route and dev button after final polish | Cleanup | `lib/spike/body_map_spike.dart` and the Profile menu button that navigates to it are still present. Originally planned to remove at T5a commit, but keeping them useful as a minimal-dependency test harness for verifying each new GLB export from Blender during the #93 re-split work. Remove at the end of the re-split + polish pass — before Sprint 11 starts. Added Apr 23, 2026. |
| 103 | `users.max_streak` column + reinstate streak wins | Backend | T5b dropped the streak branch from `services/bodyMapService.js` recent-wins because we don't track max-ever streak — without it, "current streak" isn't a personal-best claim. Small migration: add `users.max_streak INT DEFAULT 0`, backfill by computing historical max per-user from the `sessions` table (one query, idempotent). Then restore the streak-win emit branch — only fire when current streak ≥ max_streak (true PB) and add `UPDATE users SET max_streak = $current` on every session log. Blocks: nothing. Unblocks: third category of home-page wins (flame icon). Added Apr 24, 2026. |
| 104 | Bodyweight exercise volume contribution for muscle heatmap | Data | T5b's `getMuscleVolumes` treats `weight IS NULL` as 0 volume — bodyweight-heavy users (calisthenics, push-up/pull-up dominant) read as fresh across the entire heatmap regardless of what they trained. Fix: per-exercise bodyweight coefficient (e.g. push-up = 0.65 × BW, pull-up = 1.0 × BW, dip = 0.85 × BW) multiplied by the user's latest `body_measurements.weight_kg`. Requires tagging the ~50-100 bodyweight exercises with coefficients (research-backed table exists in published lit). Medium lift. Blocks: heatmap accuracy for bodyweight-heavy users. Added Apr 24, 2026. |
| 105 | Yoga pose library dedup audit (systematic) | Data cleanup | T5b cleaned 11 obviously-dirty rows (empty / prose-only `target_muscles`). The three merged sources (`yoga-api`, `yogism`, `huggingface`) likely produced more near-duplicates with **clean** muscle data that escaped the dirty-token filter. Build a near-match detector (Levenshtein on English name + Sanskrit name, normalized), flag likely dupes, review manually, then merge (re-point `session_exercises` FKs) or delete. Low priority — app works as-is. Cost: duplicates dilute flexibility region scoring (one yoga session counts twice if pose has two DB entries) and bloat the pose picker. Added Apr 24, 2026. |
| 106 | Sanskrit name normalization sweep | Data cleanup | Numeral spelling inconsistency across yoga sources: "One" vs "I", "Two" vs "II", "A/B/C" vs "I/II/III". T5b found three (Warrior One/Two/Three were unrecognized dupes of Warrior I/II/III because the Sanskrit fields used spelled-out numerals). Cheap one-time regex-assisted pass: normalize `Virabhadrasana One` → `Virabhadrasana I`, `Salamba Sirsasana One` → `Salamba Sirsasana I`, etc. Run as a migration script under `server/scripts/`, gate behind a manual review of the proposed mappings (some are legitimately different poses). Added Apr 24, 2026. |
| 108 | Figure color / background / overall visual language pass | UX v2 | T5c-a phone testing required two consecutive contrast bumps to the heatmap "Fresh" band (#C8C8C8 → #A8A8A8 → #808080) before the figure read clearly against the cream `#FAFAF7` background. The combination of cream BG + pure-gray base reads clinical/medical rather than warm-fitness. Don't iterate further on the CharacterZone anatomy mesh's color palette in isolation — bundle this with the Tripo AI personalized character work (item #67) so visual language is reconsidered against a custom-rendered figure rather than a generic one. Open questions for that pass: (a) cream → off-white or subtle gradient? (b) heatmap ramp anchored to brand colors vs current coral progression? (c) is "0-20 = fresh gray = base" the right semantic, or should "untrained" muscles use a different visual treatment (e.g. desaturated coral) to suggest "could be trained" instead of "absent"? Added Apr 24, 2026. |
| 110 | interactive_3d lighting/exposure config | Tech v2 | 4 rounds of base color iteration (#C8C8C8 → #A8A8A8 → #808080 → #606060) in T5c-a confirmed the native renderer's default lighting (Filament on Android, GLTFSceneKit on iOS) overrides the Dart-side `PatchColor` albedo — same washed-out output regardless of how dark the constant was set. Research package-level controls: exposure / IBL / specular dampening. If `interactive_3d` doesn't expose these knobs, options are (a) an upstream PR adding the config surface, or (b) Plan B — swap to Thermion (already the documented fallback in `project_sprint10_t5a.md`). Companion to #111 (material-level fix). Added Apr 24, 2026. |
| 111 | Bake base PBR material into GLB in Blender | Tech v2 | CharacterZone anatomy mesh ships without an authored PBR material — the renderer falls back to default lighting which washes out whatever albedo the Dart side sets (see #110). Fix path: during the #93 re-split session in Blender, assign a `#606060` matte PBR material (low specular, no metallic, mid-roughness) to the base body mesh before re-exporting the GLB. Alternative/companion to #110 — whichever lands first resolves the cream-background wash-out. Bundle with #93 so the re-split and the material bake happen in the same Blender session. Added Apr 24, 2026. |
| 131 | Quick Tools section on library page | UX / Approach 5 | Add a filter or section at the top of the existing breathwork library page surfacing techniques where max duration ≤ 2 minutes. Gives one-tap access to reactive tools (Physiological Sigh, Stress Reset, Simhasana, Pain Management, Appetite Control, Craving Interrupt) without needing a dedicated reactive-moment screen. ~1-day Flutter ticket. Post-Sprint 11. Decided during S11-T2 framework planning Apr 27, 2026. |
| 132 | Reactive moment surface | Feature / Approach 5 | Dedicated screen for "I need 30 seconds RIGHT NOW" use case — user opens app in distress, taps one button, gets a quick tool (physiological sigh, stress reset). Differentiator vs. major fitness apps (Hevy, Down Dog, Calm, Headspace, Apple Fitness — none have this surface). Fits Approach 5's state-focus philosophy. Post-Sprint 16 consideration. May overlap conceptually with the suggestion engine; design carefully so they don't duplicate. Decided during S11-T2 framework planning Apr 27, 2026 — deferred in favor of the simpler #131 (Quick Tools filter on library) which gets ~80% of the value. |
| 133 | Convention 4 amendment — cluster boolean uniformity, duration divergence allowed | Algorithm / Approach 5 | Design decision artifact from S11-T2 v3 spec lock (Apr 27, 2026). Convention 4 (ratio-cluster consistency) was amended in v3: techniques sharing a protocol ratio (e.g., 4-7-8 cluster: #17 4-7-8 Breathing + #36 Sleep Preparation Breath; 4-0-8-0 cluster: #22, #24, #40, #42; 4-4-4-4 cluster: #12, #16, #41; 5-5-5-5 cluster: #27, #44) get the same `pre_workout_compatible` / `post_workout_compatible` / `standalone_compatible` booleans, BUT may diverge on per-difficulty duration ranges when context-of-use justifies it. Canonical example: #17 4-7-8 capped at 5-8 min advanced (Weil's strict clinical-tool protocol); #36 Sleep Preparation extends to 5-15 min advanced (bedtime use legitimizes longer durations because the goal is "until sleep arrives"). Implication for suggestion engine (S11-T3 + Sprint 12): when surfacing technique alternatives, dedupe by protocol ratio for booleans (don't suggest 4 cluster mates as alternatives — they're physiologically the same), but respect per-difficulty duration when sizing the user's session. Captured in `Trackers/S11-T2-tagging-spec.md` (v3) Convention 4. Added Apr 27, 2026. |
| 134 | Unified seed runner / aggregator | DevOps | Surfaced during S11-T3 build (Apr 27, 2026). The S11-T3 prompt assumed an aggregator runner at `server/src/db/seed.js` that would chain individual seeds in dependency order; the actual codebase pattern is one npm script per seed (`db:seed`, `db:seed:strength`, `db:seed:breathwork`, now `db:seed:focus-areas`). T3 matched the existing per-script style. As Sprint 12 adds engine-derived seeds and Sprint 13+ multiplies them, an aggregator with explicit dependency edges (e.g. `seed-focus-areas` requires `seed-breathwork-techniques` to have run first because of the breathwork-name lookup) becomes worthwhile. Action: add a `seed:all` runner that resolves a topo-sorted dependency graph, runs each in order, and fails fast on any seed error. Alternative: leave per-seed scripts and document the manual run order in a README. Priority: Low until seed count crosses ~6-8. Added Apr 27, 2026. |
| 135 | Per-sprint audit harness pattern | DevOps | Surfaced during S11-T3 build (Apr 27, 2026). T3 produced 3 dated one-shot helper scripts in `server/scripts/` (`preflight-s11-t3-2026-04-27.mjs`, `verify-s11-t3-schema-2026-04-27.mjs`, `spotcheck-s11-t3-2026-04-27.mjs`) — pre-flight column/category checks, post-migration schema verification, and acceptance-query spot-checks. Same shape as prior dated scripts (`populate-yoga-muscles-2026-04-24.mjs`, `cleanup-dirty-yoga-2026-04-24.mjs`). Each ticket reinvents the boilerplate (dotenv, pool import, dated filename). Could consolidate into a single `server/scripts/audit/` harness with a tiny CLI that loads ticket-specific check modules and prints a consistent report. Would also let CI run all assertion queries on every push (today they only run when a human explicitly executes the dated script). Priority: Low — the per-ticket dated style works fine while ticket cadence is 1-2/week. Reconsider when Sprint 12+ produces 5+ tickets touching DB state. Added Apr 27, 2026. |
| 136 | `users.sex` column for sex-specific strength thresholds | Schema / Onboarding | T4 ships with male strength-ratio thresholds applied to all users (under-promotes some female users; never over-promotes). Add `users.sex` so `recompute_user_pillar_level` can branch correctly for the strength performance gate (Path B). Fold into onboarding work (FUTURE_SCOPE #35) — the question "what's your sex" is a natural fit for the onboarding quiz. Once shipped, update the strength branch in the function to read `users.sex` and apply female thresholds (bench ≥ 0.65× / squat ≥ 1.0× / deadlift ≥ 1.25× BW for intermediate; bench ≥ 1.0× / squat ≥ 1.5× / deadlift ≥ 1.75× BW for advanced). Added Apr 27, 2026 from S11-T4 build. |
| 137 | Cron job for nightly `recompute_all_user_pillar_levels` | DevOps / Performance | T4 ships the inference function but no automation. The Sprint 12 suggestion engine will call `recompute_all_user_pillar_levels(user_id)` on demand. If load profile shows recompute-per-home-page-load is expensive (the function does ~10 read queries per pillar = ~30 queries per call), move to nightly cron + cached read pattern. Decide based on S12 query patterns once the engine is wired up. Cheap to add later — function already exists and is idempotent. Added Apr 27, 2026 from S11-T4 build. |
| 138 | Yoga skill gate via pose prerequisites | Algorithm | T4 yoga inference uses frequency-over-time only (sessions ≥ 25 + weeks ≥ 12 for intermediate; ≥ 100 + ≥ 78 for advanced). Once FUTURE_SCOPE #33 (pose prerequisites system) ships, add a skill-content path to yoga level promotion (e.g. "user has held shoulder stand 5+ times → eligible for advanced regardless of session count"). Currently yoga is the pillar with the weakest promotion signal — frequency alone misses dedicated practitioners with shorter but high-skill histories, and over-credits long-but-shallow practitioners. Added Apr 27, 2026 from S11-T4 build. |
| 139 | Bodyweight exercise coefficient for strength performance gate | Algorithm | T4's strength performance gate (Path B) requires `body_measurements.weight_kg` AND a barbell-lift 1RM (Bench / Squat / Deadlift). Bodyweight-only lifters (calisthenics, push-up/pull-up dominant) cannot trigger Path B and are stuck on the 8-week volume floor (Path A). Adds to existing FUTURE_SCOPE #104 (bodyweight contribution for muscle heatmap) — same root problem, different surface. Resolve both together with a per-exercise bodyweight coefficient table (push-up = 0.65 × BW, pull-up = 1.0 × BW, dip = 0.85 × BW, etc.). Once available, treat coefficient × BW × max_reps as a virtual 1RM that can fire Path B for strength inference. Added Apr 27, 2026 from S11-T4 build. |
| 140 | Level-change events emitted as home-page wins | UX | When inference promotes a user (beginner → intermediate or intermediate → advanced), emit an event the home page can surface as a "win" alongside FUTURE_SCOPE #103 (streak wins). Drives engagement around progression milestones — users who don't notice their level change won't celebrate it. Implementation: add a `pillar_level_promotions` log table (or fold into a generic `user_events` table), insert a row inside the function's promotion branch, surface via the recent-wins endpoint (extends `services/bodyMapService.js`). Pre-requisite: T4 must run periodically or be called from a session-finish hook so promotions actually fire. Added Apr 27, 2026 from S11-T4 build. |
| 141 | Equipment column + filtering on `exercises` table | Schema / Data | The `exercises` table has no queryable equipment column — equipment info is implicit (embedded in names like "Barbell Bench Press" and present in the original `free-exercise-db` JSON seed but never normalized). Fine today because nothing queries by equipment; becomes blocking when any of these land: (a) **"filter by available equipment" UI** in the composer (Sprint 14+) — Hevy-style "I only have dumbbells today, show me dumbbell-compatible exercises"; was in the original Blueprint v3 strength scope but never implemented; (b) **equipment-aware engine signals** — any future weight formula or filter that wants equipment as input; (c) **onboarding equipment capture** — companion to FUTURE_SCOPE #44; the column is needed to enforce a user-declared "I train at home with dumbbells + bands only" preference downstream. **Implementation path:** add `equipment VARCHAR(40)` (or `TEXT[]` if multi-equipment is meaningful), backfill via either re-pull of `free-exercise-db` JSON joined by name/id (cleaner) or deterministic name-parse looking for `barbell`/`dumbbell`/`cable`/`machine`/`bodyweight`/`kettlebell`/`band`/`smith` (faster), add `idx_exercises_equipment`, audit + spot-check (S11-T2 / S11-T3 pattern). **Why parked:** v1 suggestion engine (Sprint 12) doesn't use equipment; Strength tab today doesn't filter by equipment; no current user feedback flagging it. **Trigger to pull off the shelf:** the first sprint that wants any of the three use cases — likely Sprint 14 (composer) or whichever sprint adds onboarding equipment capture. Surfaced during S12-T1 pre-flight (Apr 28, 2026) when the v1 engine was confirmed not to need it. Added Apr 28, 2026. |
| 142 | Yoga catalog coverage gap for body focuses at beginner level | Content / Approach 5 | S12-T2 smoke test surfaced that several body focuses have <3 beginner-level yoga poses tagged via `focus_muscle_keywords` substring match against `exercises.target_muscles`. Confirmed pool sizes at beginner level (Apr 28, 2026): biceps=1, triceps=6, chest=11; healthy: shoulders=16, back=16, quads=22, calves=30, glutes=32, hamstrings=34, core=39. Symptom: yoga-tab and home/cross-pillar sessions degrade — biceps/yoga_tab/30 returns 26 min vs 30 budget (warmup picks the only pose, main reuses it, cooldown empty after de-dup). Engine handles honestly (degrades phases + reports deficit in `metadata`), so this is a UX polish issue not a correctness blocker. **Fix path:** audit the yoga library row-by-row for body-focus coverage at beginner level, then either (a) re-tag existing poses where the keyword substring isn't currently present in `target_muscles` despite the pose actually targeting that muscle, or (b) add new beginner-level poses for under-covered focuses (especially biceps — 1 pose is structurally too few for any phase split). Adjacent issue (separate cleanup): the `focus_muscle_keywords` seed has 13 dead entries that match 0 exercises via substring (`biceps brachii`, `gastrocnemius`, `vastus`, etc. — full list in S12-T2 SPRINT_TRACKER notes); resolving those alongside this audit may close some of the coverage gap without adding new poses. **Target:** ≥3 beginner-level yoga poses per body focus (matches yoga-tab/15 minimum main count). Sprint 13+. Surfaced during S12-T2 build (Apr 28, 2026). Same class of issue as #143 — content thinness surfaced by smoke test. Fix: author more beginner-tier yoga poses for body focuses with sparse coverage. Added Apr 28, 2026. |
| 143 | Energize state focus: beginner main pool = 1 (Morning Energizer only) | Content / Approach 5 | Surfaced from S12-T3 pre-flight inspection. All other beginner-eligible energize techniques (Bhastrika, Kapalabhati, Surya Bhedana, Wim Hof, Cyclic Hyperventilation) are intermediate+ by design — energizers are forceful and not appropriate for unprepared beginners. Under v2 spec the engine never throws (range-bracket picker only offers honestly-fillable options), but beginner energize users see only the 0–10 min bracket as available; 10–20 / 21–30 / 30–45 are all locked-by-level until they level up. Fix: author 2–3 gentle beginner-safe energize techniques (Three-Part Breath with inhale emphasis, slow Bhastrika at modified pace, paced Cyclic Hyperventilation lite). Sprint 13+ content authoring. |
| 144 | Default-bracket selection from history (state-focus picker) | Engine / UX / Approach 5 | Picker default = "largest bracket the user has previously completed." Requires reading recent breathwork sessions and mapping their durations to brackets. Wire in T3.5 if simple; otherwise Sprint 13 picker UI work. Surfaced from S12 v2 spec followup #14. |
| 145 | Reflection-phase user prompts | UX / Approach 5 | Reflection is a silent timer in v1. UX could rotate prompts ("notice how you feel now," "set an intention for the next hour"). Sprint 13+ design. Surfaced from S12 v2 spec followup #15. |
| 146 | Endless mode session-length defaults from history | Engine / Approach 5 | First-time endless users get centering=2, reflection=2. Returning endless users could inherit their average actual practice duration. Sprint 13+ enrichment. Surfaced from S12 v2 spec followup #16. |
| 147 | 45–60 min bracket for state-focus picker | Engine / Content | Currently no state-focus content reaches 60 min (only Breath Counting reaches 45 at advanced; nothing reaches 60). Add a 5th numbered bracket when content motivates it. Decision deferred until then. Surfaced from S12 v2 spec followup #18. |
| 148 | Energize beginner: 21-30 and 30-45 brackets empty | Content / Approach 5 | Beginner energize users see only `0-10` and `10-20` brackets in the picker (plus `endless`). 21-30 and 30-45 are hidden because no beginner-tier energize technique reaches those durations at any level — they're not just locked, they're empty. Closely related to FUTURE_SCOPE #143 (beginner main pool = 1: Morning Energizer). Fix: author or extend energize techniques with beginner-tier columns reaching 20+ minutes. Could overlap with #143's beginner-safe energizer authoring (Three-Part Breath with inhale emphasis, paced Cyclic Hyperventilation lite). Sprint 13+ content authoring. Surfaced from S12-T3.5 pre-flight diagnostic (Apr 29, 2026). |
| 149 | Short-duration locked for intermediate/advanced focus and recover users | **Open question / UX decision (NOT scheduled work)** | Intermediate and advanced focus users, and advanced recover users, see the `0-10` bracket as **empty**, not available. This happens because the higher-tier columns for these focuses are designed for longer protocols (Box Breathing intermediate = 10-15, etc.); the user *can* do shorter sessions but only at lower-tier columns the engine doesn't surface to them. Two possible interpretations: (a) **Bug** — an advanced user should still be able to do a 5-min session; fix at the engine layer to allow lower-tier columns when no user-level columns fit a bracket. (b) **Feature** — advanced users have outgrown 5-min sessions for these focuses; UI shows "Higher tiers practice longer — try a longer session." **Decision deferred to Sprint 13 picker UX design.** Current behavior (empty bracket = hidden in picker) is honest about the data. No build work scheduled until the UX call is made. Surfaced from S12-T3.5 pre-flight diagnostic (Apr 29, 2026). |
| 150 | Sleep techniques don't reach 25+ minutes at any level | Content | Sleep pool (Deep Sleep Induction, Sleep Preparation, 4-7-8 cluster) tops out at advanced ~20 min. The 30-45 bracket is empty for all three levels. The 21-30 bracket is empty at beginner and locked-by-advanced at intermediate. Fix: author or extend sleep techniques to reach 25-40 min — could be longer Yoga Nidra-style guided sequences or extended 4-7-8 advanced protocols. Sprint 13+ content authoring. Surfaced from S12-T3.5 pre-flight diagnostic (Apr 29, 2026). |
| 151 | Time-budget reallocation for mobility-home when strength-main pool is empty | Engine / Approach 5 | Original spec line 668: "time budget reallocates: the strength-main slot's duration goes to extending the yoga main." T4 deferred this — strength-main minutes simply disappear from the session total when the pool is empty, with `metadata.estimated_total_min` reflecting the shortfall honestly. Amendment 1 made the empty-pool case structural (strength `practice_types` is empty across the entire pillar) so the reallocation question becomes: should mobility-home's yoga main get a longer time-slot since there's never any strength to share with? A reallocation pass would extend yoga-main duration to absorb the strength-main minutes. Requires a duration-reallocation block we don't have today. Sprint 13+ refinement. Surfaced from S12-T4 build (Apr 29, 2026). |
| 152 | Hide `mobility` from the strength-tab focus picker (Flutter UI) | UX / Approach 5 | The engine throws `RangeError` if `(focus='mobility', entry_point='strength_tab')` is called; the picker UI in Sprint 13+ should not surface the option in the first place. UI-layer change only; engine is already correct. Sprint 13 ticket. Surfaced from S12-T4 build (Apr 29, 2026). |
| 153 | Movement-quality tagging for yoga | Content / Schema | T2 surfaced and T4 confirmed: `exercises.practice_types` is a TEXT[] of yoga STYLE labels (vinyasa, hatha, yin, restorative, sun_salutation), not movement-quality tokens (mobility, flexibility, dynamic, static). Add a `movement_quality` column or parallel tag taxonomy and tag the 258 yoga rows. **When this lands, retire the practice-type style remap codified in `S12-T4-AMENDMENT-1` and replace the engine queries with the original spec predicates.** This amendment exists because of this gap; the amendment's retirement is the closing of the loop. Sprint 13+ content/schema work. Surfaced from S12-T4 pre-flight (Apr 29, 2026). |
| 154 | Compound-detection accuracy | Algorithm / Schema | `array_length(target_muscles, 1) >= 3` is a coarse compound proxy used by full_body. Some 3-muscle exercises are isolation-by-adjacency (e.g. concentration curl that secondarily engages forearms and shoulders). A `is_compound BOOL` column tagged by domain knowledge would beat the heuristic. Sprint 13+ tagging work. Surfaced from S12-T4 build (Apr 29, 2026). |
| 155 | Endurance / cardio focus area | Algorithm / Content | Approach 5 names cardio as a focus-area trainable through any pillar, but no `endurance` or `cardio` row exists in `focus_areas` today. Decide whether this is a new body focus (with its own muscle keywords or compound rule), a state focus (with breathwork main), or a third focus type. Pre-launch decision; post-Sprint-12. Surfaced from S12-T4 spec writing (Apr 29, 2026). |
| 156 | `target_muscles` text-to-array migration | Schema / Performance | T2/T4 both work around the text storage with `STRING_TO_ARRAY` at query time. A migration to `TEXT[]` would (a) let us drop the `STRING_TO_ARRAY` wrapper, (b) gain GIN-indexable membership tests, (c) clean up the substring-vs-token collision risk. The `compoundFilter()` helper is a single-line change site if this migration lands. Sprint 13+ schema cleanup. Surfaced from S12-T4 build (Apr 29, 2026). |
| 157 | Mobility strength-main pool authoring | Content | The original spec called this pool "rare; likely empty." Amendment 1 confirmed it's structurally always 0 — strength rows have no `practice_types` data at all (the column is empty for the entire strength pillar). If product wants mobility sessions to ever include a strength element (e.g. weighted carries, suitcase walks, banded mobility under load), Sprint 13+ should author 5-10 strength rows tagged with `practice_types ILIKE '%mobility%'` (or whatever the FUTURE_SCOPE #153 movement-quality column ends up being). T4 itself doesn't depend on this; the engine handles either case via the structural always-skip. Surfaced from S12-T4 spec + Amendment 1 (Apr 29, 2026). |
| 158 | Deprecate practice-type style remap (Amendment 1 retirement) | Algorithm / Approach 5 | When FUTURE_SCOPE #153 (movement-quality tagging) lands, the engine's `WARMUP_PRACTICE_STYLES`, `MOBILITY_MAIN_STYLES`, and `COOLDOWN_PRACTICE_STYLES` constants — codified per `Trackers/S12-T4-AMENDMENT-1-practice-type-remap.md` — should be replaced with direct queries against the new movement-quality tags (`mobility`, `flexibility`, `restorative`, etc.). The amendment doc itself can be archived at that point. The engine header comment block + the constants point back to this entry as the closing-of-the-loop trigger. Sprint 13+ when #153 ships. Surfaced from S12-T4 Amendment 1 (Apr 29, 2026). |
| 159 | Strength `practice_types` is empty for the entire pillar — drop or author? | Schema | Confirmed in S12-T4 pre-flight: 0 strength rows have any `practice_types` data. Either drop the column for strength rows (and adjust any read sites), or author the data (mobility / olympic / hypertrophy / etc.). Decide based on whether the engine ever wants strength-side practice-type filtering. Currently nothing reads it for strength. Sprint 13+ schema decision. Surfaced from S12-T4 Amendment 1 (Apr 29, 2026). |
| 160 | Suggestion engine architecture extraction (sprint-close housekeeping) | Refactor / DevOps | ✅ **CLOSED — shipped as S15-T4 (May 17, 2026, feat `9aa95d6`).** Engine extracted from the 1791-LOC monolith into `server/src/services/suggestion-engine/` with 12 files (the spec's proposed 11 + a separate `recency.js` for the body-focus T5 helper). Two recipe files (`cross-pillar.js` 467 LOC, `yoga-only.js` 310 LOC) intentionally over the 300-LOC guideline with documented inline justification — variant cohesion preferred over per-variant splits. Smoke 3537/0 with no new failure categories vs baseline. Public API surface preserved (BRACKET_TABLE, NotImplementedError, checkRecencyOverlap, getAvailableDurations, generateSession). `errors.js` ships `EngineContractError` as a shell — its wiring to existing RangeError throw sites is S16-T2's job. Original entry text below preserved for posterity. — Engine file `server/src/services/suggestionEngine.js` is now ~1140 lines after T2/T3.5/T4 layered work. T2 / T3.5 / T4 `/review` all flagged sprint-close extraction as the right time to consolidate. Proposed structure: `server/src/services/suggestion-engine/` directory with `index.js` (dispatch + exports), `constants.js` (BRACKET_TABLE, style sets, picks tables), `helpers.js` (compoundFilter, durationsForLevel, level rank), `pickers.js` (5 picker functions), `recipes/cross-pillar.js`, `recipes/strength-only.js`, `recipes/yoga-only.js`, `recipes/state-focus.js`, `recipes/available-durations.js`, `item-formatters.js`. Net effect: same logic, surface area easier to navigate; per-recipe tests can target individual files; T7's HTTP layer imports from the index without pulling in the whole module graph at once. **Trigger:** Sprint 12 close, after T7 ships. Pure refactor — zero behavior change. Surfaced from S12-T2 / S12-T3.5 / S12-T4 reviews (cumulative recommendation). Added Apr 29, 2026 from S12-T4 build. |
| 161 | Personalized `alternative_focus_slug` in recency warnings | Engine / Approach 5 | T5 hardcodes `alternative_focus_slug = 'recover'` in every `recency_overlap` warning per spec Decision 7 — `recover` is always-safe and requires no recommendation model. Personalized alternatives (e.g. "you did chest yesterday → try `mobility` today" based on the user's least-recently-trained focus + opposite-bias from yesterday) need the broader personalization-algorithm planning that gates the suggestion ranker. Sprint 14+ once that planning lands. Surfaced from S12-T5 spec (Apr 29, 2026). |
| 162 | Recency UNION refactor when unified-session model lands or any path writes body-focus slugs to `breathwork_sessions` | Engine / Schema | T5 collapsed the spec's 3-arm UNION (`sessions` ∪ `yoga_sessions` ∪ `breathwork_sessions`) to a single-table `SELECT` against `sessions` — the reasons are (a) `yoga_sessions` doesn't exist in this codebase (yoga writes to `sessions` with `type='yoga'`, so the yoga arm is structurally redundant), (b) `breathwork_sessions` has no `date` column to fit the recency window shape AND state-focus rows are excluded by the overlap rule AND no v1 path writes body-focus slugs there. The `breathwork_sessions.focus_slug` column was added anyway for forward-compat analytics. **Trigger:** if any future surface starts writing body-focus slugs to `breathwork_sessions` (e.g. a "breathwork-as-warmup" body-focus pattern), the recency query needs the breathwork arm restored — keyed off `created_at::date` since the table has no `date` column. Alternatively, the unified-session refactor (FUTURE_SCOPE #114) would consolidate all session writes into a single table and the UNION question dissolves entirely. Surfaced from S12-T5 build (Apr 29, 2026). |
| 163 | Rate limit `/api/exercises/:id/exclude` and `/api/exercises/:id/keep-suggesting` | Hardening / Server | Both endpoints are user-state mutations with no rate limiting today. `/review` on S12-T6 flagged this as W1 and it was deferred as out-of-T6-scope. Should fold into a Sprint 13/14 endpoint-hygiene pass alongside auth-failure rate limits and other user-mutation surfaces. Surfaced from S12-T6 build (Apr 30, 2026). |
| 164 | Audit + remove unused `PUT /api/workout/exercise-pref` endpoint | Cleanup / Server | React-PWA-era persistent-preference write at `server/src/routes/workout.js:266`. Flutter call-pattern verification during S12-T6 build (`grep` across `app/lib/` for `exercise-pref` returned 0 matches) confirmed no current client uses it. Endpoint is dead surface; safe to remove pending one more confirmation pass that no in-flight Sprint 13 work depends on it. Surfaced from S12-T6 Flutter call-pattern pre-flight (Apr 30, 2026). |
| 165 | Full breathwork-session phase persistence | Engine / Schema | T7's `GET /api/sessions/last?focus=<slug>` formatter (`server/src/services/sessionFormatter.js`) reconstructs breathwork sessions as a single-phase `practice` synthesis with `metadata.partial_reconstruction: true` because `breathwork_sessions` v1 only stores `technique_id` (no per-phase row table). The engine's live state-focus output emits 3 phases (`centering / practice / reflection`); replay can't honor that without a persistence-layer change. Two paths: (a) add a `breathwork_session_items` joining table + write 3 rows on completion (`session.js`/`breathwork.js` write sites); (b) denormalize per-phase JSONB onto `breathwork_sessions` mirroring `sessions.phases_json` for 5-phase. Once either lands, drop `partial_reconstruction` from breathwork `/last` responses and remove the formatter's defensive single-phase branch. **Trigger:** telemetry shows users want exact 3-phase replay on Repeat Last, OR Sprint 13's breathwork player UI surfaces an inconsistency between `/suggest` (3 phases) and `/last` (1 phase) that hurts UX. Surfaced from S12-T7 build (Apr 30, 2026). |
| 166 | Typed-error refactor in `suggestionEngine.js` | Engine / Refactor | T7's HTTP error mapper (`mapRangeErrorToCode` in `routes/sessions.js`) string-matches against `RangeError` message text emitted at 10 throw sites in `server/src/services/suggestionEngine.js`. Pre-flight (`scripts/preflight-s12-t7-shape.mjs` check (c4)) inventories the exact throw messages and halts on drift, but the substring approach accumulates fragility — a future engine refactor that rephrases a throw message would silently bucket into `unmapped_engine_error` without /review noticing until Flutter starts seeing it. Fix: replace the 10 `throw new RangeError(...)` sites with `throw new EngineContractError({ code: 'invalid_focus_entry_combo', detail: ... })` typed-error class. T7's mapper becomes a pass-through `err.code` read. ~30-line refactor across the 10 throw sites; the pre-flight check (c4) becomes a class-existence check rather than a substring inventory. Pairs naturally with FUTURE_SCOPE #160 (engine architecture extraction at sprint close) — best done as part of the same refactor pass. **Trigger:** Sprint 12 close housekeeping or first time a new engine throw is added that doesn't fit existing substrings. Surfaced from S12-T7 build (Apr 30, 2026). |
| 167 | Validate `exercises[].id` existence + `type='strength'` on save endpoints | Hardening / Server | Cross-endpoint hardening: BOTH T7's `POST /api/sessions/save-as-routine` AND the existing `POST /api/routines` accept caller-supplied `exercise_id` values without verifying that (a) the row actually exists in `exercises` (DB FK catches this on insert, but the error surfaces as a 500 instead of a clean 400 with `invalid_strength_exercise_ids`), and (b) the exercise has `type = 'strength'`. A misuser could save a routine containing yoga rows or breathwork rows by manipulating the request body — the rest of the app presumes `user_routines` are strength workouts and would render garbage when run through the routine player. T7 mirrors this gap rather than introducing a new one (existing `POST /api/routines` from Sprint 8 has the same shape since day 1). Fix on both endpoints: pre-INSERT bulk SELECT against `exercises WHERE id = ANY($1::int[]) AND type = 'strength'`, reject 400 with `invalid_strength_exercise_ids` if the count diverges from the request size. **Trigger:** Sprint 13/14 endpoint-hygiene pass alongside FUTURE_SCOPE #163 rate-limiting work, OR first time a UI bug or API client surfaces a non-strength routine in the player. Surfaced from S12-T7 build (Apr 30, 2026). |
| 168 | Centralize test-fixture sentinel pattern (`smoke-fixtures.mjs` helper) | Tooling / Tech debt | T7 introduces two parallel sentinel patterns in `server/scripts/test-suggestion-engine-t2.js`: `notes='t7-smoke-fixture'` for `sessions` rows (cleanup deletes by sentinel) and throwaway `__t_state_test` / `__t_body_test` slugs in `focus_areas` (cleanup deletes the focus rows + cascading session/breathwork rows). T6 had a different snapshot/restore variant for `exercise_swap_counts` + `user_excluded_exercises`. T5 had its own tagged-row pattern for the recency block. Three different conventions across three sub-blocks of the same rolling smoke harness. A `server/scripts/lib/smoke-fixtures.mjs` helper (or similar) would centralize: (i) sentinel-tagged INSERT, (ii) bulk DELETE-by-sentinel cleanup, (iii) snapshot/restore for tables without a sentinel column, (iv) try/finally + SIGINT/SIGTERM lifecycle wiring. Drives consistency across future smoke blocks and lowers the cost of the next ticket's HTTP-layer testing. **Trigger:** Sprint 13 or first new smoke block that needs the same lifecycle plumbing — at that point the marginal value of extracting matches the cost. Surfaced from S12-T7 build (Apr 30, 2026). **[2026-05-17 verification note]** S15-T6 pre-flight searched `server/scripts/lib/` and found only `prod-guard.mjs` — no `smoke-fixtures.mjs`. The sentinel pattern remains inline in `server/scripts/test-suggestion-engine-t2.js` (20 occurrences) and `server/scripts/t1-curl-roundtrip.mjs`. Either the S13-T7 extraction never landed, or it landed under a different filename. Status: helper does not currently exist as a shared module. Tracked further in FS #253. |
| 169 | Rename `focus_areas.focus_type → type` + `focus_areas.sort_order → display_order` | Schema / Tech debt | Surfaced from S13-T2 build (Apr 30, 2026). Pre-flight diagnostic caught spec-vs-reality drift: spec referenced `type` and `display_order` as DB columns; live DB columns are `focus_type` and `sort_order` from S11-T3 seed. Resolved at the route layer with `SELECT focus_type AS type, sort_order AS display_order` aliases — client contract uses the cleaner names; one-line route comment notes the legacy aliases. Future cleanup: rename DB columns in a clean migration, drop the aliases, drop the comment. ~15-line migration in `server/src/db/migrate.js` + same-line edits to `server/src/services/suggestionEngine.js:202` (where `is_active` filter sits) and `server/src/routes/focus-areas.js`. Idempotent. **Trigger:** any future sprint that touches `focus_areas` schema for another reason — fold the rename into that migration to avoid a standalone schema-touch sprint. Don't pull off the shelf as a standalone ticket; the alias is durable until a triggering reason arrives. Added Apr 30, 2026. |
| 170 | Extract `LevelCaptureScaffold` widget to dedupe 3 onboarding pages | Flutter / Polish | Surfaced from S13-T1 `/review` (Apr 30, 2026, Grade B+ → A-). The 3 onboarding pages (`level_capture_strength_page.dart`, `level_capture_yoga_page.dart`, `level_capture_breathwork_page.dart`) share ~80% of their structure: app bar with step indicator, headline, 3 vertical chips with descriptions, Continue button. Per Decision 6 of the original T1 prompt ("functional, not polished"), this duplication was an explicit spec choice and not addressed in T1. Future: extract the shared scaffold into a reusable `LevelCaptureScaffold` widget that takes pillar + chip definitions + onContinue callback. ~10-min Flutter refactor. **Trigger:** when next polish/refactor sprint touches onboarding (likely Sprint 15 when full onboarding flow lands — Decision 6 changes from "functional" to "polished" at that point). Added Apr 30, 2026. |
| 171 | Extract `kPillarLevels` + `kPillars` constants to a shared file | Flutter / Type safety | Surfaced from S13-T1 `/review` (Apr 30, 2026, suggestion). The strings `'beginner'/'intermediate'/'advanced'` and `'strength'/'yoga'/'breathwork'` are hardcoded in 3 page files. Server-side `users.js` validation also hardcodes them. A shared constants file at `app/lib/config/pillar_levels.dart` (or similar) would survive both Flutter and server-side type drift if shared across both layers. Server-side equivalent: `server/src/constants/pillars.js`. Worth pairing both with a comment cross-referencing the other to keep them in sync. **Trigger:** whenever next ticket touches level/pillar validation OR when Sprint 15 lands full onboarding (whichever comes first). Added Apr 30, 2026. |
| 172 | Body-focus matrix flake investigation — 9 deterministic fails on home/30 budget | Backend / Smoke | Surfaced during S13-T1 + T7 smoke runs (Apr 30, 2026). 9 assertions fail deterministically: `chest/home/30`, `back/home/30`, `shoulders/home/30`, `triceps/home/30`, `core/home/30`, `glutes/home/30`, `quads/home/30`, `hamstrings/home/30`, `calves/home/30` — all reporting `estimated_total_min: 37 within ±10% of budget 30 — drift=23.3%, exceeds the ±10% gate`. Pre-existing on `main`; predates Sprint 13. Could be: (a) a tagging issue in body-focus seed data (estimated_total_min calculation drifting), (b) an estimator/budgeter issue in the suggestion engine, or (c) a budget-floor issue (30-min budget too tight for these focuses with `home` equipment context). Need investigation. Smoke harness currently allows these specific 9 fails as known flake (whitelisted by name pattern in T7's pre-flight, deferred to FUTURE_SCOPE). **Trigger:** any tagging refresh sprint or before launch. **Priority:** medium — doesn't block sprint progress but does mask any new failures with the same pattern. Added Apr 30, 2026. |
| 173 | `deleteBySentinel` user_id scoping for sentinel collision defense | Backend / Smoke tooling | Surfaced from S13-T7 `/review` W1 (Apr 30, 2026, accepted-not-fixed). Current `deleteBySentinel(table, sentinelColumn, sentinelValue)` deletes WHERE sentinel matches across the whole table. The original sessions cleanup before refactor was `WHERE user_id = $1 AND notes = $2` (user-scoped). Refactor accepted the wider-scope deletion as a design choice because sentinel values like `'s12-t5-fixture'`, `'s12-t7-fixture'`, `'s13-t7-fixture'` are sprint-ticket-unique. **Risk:** if a future smoke ticket adopts a colliding sentinel, blast radius widens (could delete production user data). **Mitigation today:** sentinel hygiene — every smoke ticket uses `sentinelFor('s<sprint>-t<ticket>')` per the helper convention, and architects review sentinel choices at spec-time. **Future fix:** extend `deleteBySentinel` to accept an optional `whereExtras` map for per-user scoping when sentinel uniqueness alone isn't enough. ~5-line API extension in `smoke-fixtures.mjs`. **Trigger:** if a sentinel collision is ever observed, OR proactively when next smoke-tooling sprint lands. Added Apr 30, 2026. |
| 174 | Bulk INSERT for `restoreSnapshot` (perf optimization) | Backend / Smoke tooling | Surfaced from S13-T7 `/review` (Apr 30, 2026, suggestion 1). Current `restoreSnapshot` does N INSERT round-trips for N rows. Fine for smoke (typical N is small) but would scale poorly for large snapshots. Future: batch into a single multi-row INSERT statement using `pg`'s parameterized variadic syntax. ~10-line refactor in `smoke-fixtures.mjs`. **Trigger:** when smoke harness ever needs to snapshot/restore tables with 100+ rows. Not pressing today. Added Apr 30, 2026. |
| 175 | Setup phase in `withFixtureLifecycle` so handlers register before setup | Backend / Smoke tooling | Surfaced from S13-T7 `/review` (Apr 30, 2026, suggestion 2). Current `withFixtureLifecycle` runs cleanup setup AFTER the test block begins. Theoretically a SIGINT/SIGTERM in the brief window before signal handlers register could escape cleanup. Mitigation today: very brief window, never observed. Future: accept an optional setup phase argument and register handlers before invoking it. ~5-line API extension. **Trigger:** if a cleanup-leak is ever observed, OR proactively in next smoke-tooling pass. Added Apr 30, 2026. |
| 176 | DI for pool import in `smoke-fixtures.mjs` (testability) | Backend / Smoke tooling | Surfaced from S13-T7 `/review` (Apr 30, 2026, suggestion 3). Helper module imports `pool` as a singleton from `../../src/db/pool.js`. Makes unit-testing the helper itself awkward (would need to monkey-patch the import). Future: accept an optional `pool` argument with default fallback to the singleton — preserves call-site simplicity while allowing test injection. ~3-line API extension. **Trigger:** when next sprint adds unit tests for the helper module itself. Not pressing today since the helper is integration-tested through smoke. Added Apr 30, 2026. |
| 177 | `smoke-fixtures` helper supports LIKE/IN matching variants | Backend / Smoke tooling | Surfaced from S13-T7 `/review` (Apr 30, 2026, suggestion 5). Current helper exposes only equality matching for sentinel deletes (`WHERE col = value`). Some smoke patterns might benefit from `LIKE 'prefix%'` (e.g. all `s13-*-fixture` rows) or `IN (val1, val2, ...)` (multi-sentinel cleanup). Future: add `deleteBySentinelLike` and `deleteBySentinelIn` helper variants. ~15-line API extension. **Trigger:** when a smoke ticket needs cross-sprint cleanup or multi-sentinel cleanup patterns. Added Apr 30, 2026. |
| 178 | `restoreSnapshot` insertion ordering for FK self-references | Backend / Smoke tooling | Surfaced from S13-T7 `/review` (Apr 30, 2026, suggestion 6). Current `restoreSnapshot` doesn't impose row insertion order. Could matter for tables with foreign-key self-references (e.g. `parent_id` columns where rows reference other rows in the same table) — restore order matters to avoid FK violations. Today: none of the snapshotted tables have FK self-references, so this is theoretical. Future: accept an optional `orderBy` argument or topologically sort rows by FK dependency. **Trigger:** if a future smoke block ever snapshots a self-referencing table. Added Apr 30, 2026. |
| 179 | `SuggestServiceException.code` — enum vs free-form String | Flutter / Type safety | Surfaced from S13-T3 `/review` (Apr 30, 2026, architecture recommendation #2). Currently `SuggestServiceException` carries `code` as `String?` populated from T7's stable error code dictionary (`invalid_focus_slug`, `unknown_focus_slug`, `invalid_entry_point`, `body_focus_requires_time_budget`, `state_focus_requires_bracket`, `invalid_time_budget`, `invalid_bracket`, `invalid_focus_entry_combo`, `engine_error`, `network_error`). An enum (`SuggestErrorCode`) would give compile-time exhaustiveness checking when downstream surfaces map codes to user-facing copy via `switch(code)`. Not worth doing in T3 because no downstream `switch` exists yet — the cost is real (10 enum cases + factory + JSON deserialization round-trip + extension method for `userFacingMessage`) while the win is deferred until a caller needs exhaustiveness. **Trigger:** when T4 (or any downstream surface) writes the first `switch(code)` that maps codes to UI copy. At that point the enum's exhaustiveness catches "you forgot a case" at compile time, and the upgrade path is mechanical — service-layer factories convert from String to enum, exception field type changes, downstream switch becomes exhaustive. Until then, free-form String is honest about "this is a stable code we map at the call site, not a domain model with closed semantics." Added Apr 30, 2026. |
| 180 | Custom focus-area icon set + body-map icon | Flutter / Polish | Surfaced from S13-T4 + AMENDMENT-1 (May 3, 2026). Two related deferrals best handled as a single design + commission ticket: (a) the 17 focus-area chips in the home pie picker render with Unicode emoji placeholders today (`💪` for both biceps and triceps, `🦵` for both quads and hamstrings — duplicate-emoji collisions are intentional and documented in `kFocusEmoji` at `app/lib/pages/home/widgets_v2/_tokens_v2.dart`); (b) the body-map navigation icon at the home page top-right currently uses `LucideIcons.scan` as a stand-in (AMENDMENT-1 §2.5 swapped from a body-silhouette spec because a generic person icon collided visually with the bottom-nav Profile tab). Single ticket: design + commission custom SVGs for the 17 focus areas plus a body-silhouette icon for the body-map action; replace emoji map with asset references; replace `LucideIcons.scan` with the new SVG. **Priority:** mid — emoji is functional, this is design polish only. **Trigger:** Sprint 14/15 polish pass that touches the home page, OR when a designer becomes available. Added May 3, 2026. |
| 181 | Quads at 45-min budget engine error | Backend / Engine | Surfaced during S13-T5 device test (May 3, 2026). `(focus='quads', entry_point='home', time_budget_min=45)` returns engine error. `quads/home/30` and `quads/home/60` both work; only the 45-min cell fails. Same family of symptom as `#172` (body-focus matrix flake on home/30 budget) but a different cell — Quads isn't in #172's list of 9 failing focuses. Could be: (a) same root cause as #172 (estimator drift on certain (focus, budget) combos), (b) a Quads-specific data gap that only surfaces at the 45-minute composition, (c) a tolerance edge case where the engine narrowly misses ±10% only at this cell. Surfaces in the picker as the generic "Something went wrong. Please report." retry card — engine fails honestly, T5 surface degrades gracefully. **Investigation:** run `(quads, home, 35)` and `(quads, home, 50)` and `(quads, home, 55)` to bracket the failure. If ALL middle cells fail and only 30/60 work, root cause is "Quads has no main pool that composes cleanly between 30 and 60." If only 45 fails, narrower tolerance issue. **Trigger:** any tagging-refresh sprint or before signups open. **Priority:** medium — degrades user trust if hit during normal use. Added May 4, 2026 from S13-T5 device test. |
| 182 | Slider polish path retired | Polish / Closed | T5 base shipped a Material `Slider` for body-focus duration (continuous 30–60 with 5-min snap, haptic ticks, animated value display, tick labels at 30/45/60, `~Y min main work` subtitle). AMENDMENT-1 replaced the slider with the half-pie picker (3 wedges: 30/45/60 only). Engine still supports any 5-min step internally (35/40/50/55) and the body-focus suggested-default endpoint still snaps to nearest of [30,45,60] for the picker — meaning the data path supports finer granularity than the UI exposes. **What's lost:** ability to pick 47-min specifically; haptic feedback per minute during drag; "main work" math subtitle that gave the slider purpose. **What's gained:** unified visual language with state-focus picker; cleaner sheet; fewer interaction modes. **Closing note:** if any future product call wants finer granularity than 30/45/60 for body focus, the path is to add wedges to the half-pie (4 or 5 wedges still fit) — NOT to bring back the slider. Added May 4, 2026 from S13-T5 AMENDMENT-1 close. |
| 183 | Pie selection persistence on cold app launch | UX | Surfaced during S13-T5 fix-forward device test (May 4, 2026). The home pie remembers the user's last-tapped focus visually (e.g. Core wedge stays purple-highlighted) even after force-stopping the app and reopening to a fresh idle state. Currently this is the standard behavior — pie selection is independent of `SuggestProvider.state`. **Question:** should the pie reset to no selection on cold start, or persist last-tapped focus? Reset feels cleaner ("today is a fresh day"), persistence preserves continuity ("I was looking at Core, still am"). Not a bug — just an unstated behavior. Worth a product call before signups open. **Implementation if we change it:** clear `selectedFocus` in `HomeProvider._loadInitial` (or wherever the pie's selected state is initialized). One-line change. Added May 4, 2026 from S13-T5 device test. |
| 184 | Yoga-tab 15-min duration option | UX / Approach 5 | Originally locked out of S13-T5 scope (Decision #4 in `Trackers/S13-T5-DESIGN.md`) because the home picker is cross-pillar with a 30-min floor (5-phase math: bookend + warmup + main + cooldown + bookend can't honestly compose under 30 min). The yoga tab is single-pillar and bends to 15 min naturally. When the yoga tab gets its own duration picker (Sprint 14+ when the yoga-tab refresh lands), the picker should expose `15 / 30 / 45 / 60` minutes per the original S12 v2 engine spec line 295 (`time_budget_range` accepts these for yoga-only sessions). Could reuse the half-pie widget from T5 — 4 wedges instead of 3. **Trigger:** yoga-tab redesign sprint. Added May 4, 2026 from S13-T5 spec carryover. |
| 185 | Locked/empty wedge progression hint | UX / Approach 5 | T5 base Decision #10 said "hide unavailable brackets entirely." AMENDMENT-1 §2.7 superseded this to "render at 0.4 opacity, non-tappable" because the half-pie shape doesn't tolerate hidden wedges (creates asymmetric gaps). What's lost in the supersession: progression-as-feature surface — the user sees a faded wedge and doesn't know *why* it's faded (locked by level? content gap? user-banned exercise?). **Future polish:** tap-on-faded-wedge surfaces a tooltip explaining the lock reason. Three reason categories per the engine: (a) **locked-by-level** ("Unlocks at intermediate breathwork"), (b) **empty content** ("No 30–45 min calm sessions yet — coming soon"), (c) **excluded by user** ("You've excluded this exercise — manage exclusions in settings"). Currently silent. **Implementation:** the `available-durations` endpoint already returns `state` with these distinctions internally before the route filters down to `'available'` — surface the reason field in the API response, render a tooltip on long-press of faded wedges. **Trigger:** Sprint 14/15 polish pass on the picker. Related: closes the loop on FUTURE_SCOPE #149 (the "is it a bug or a feature" decision) by giving us the surface to communicate "feature" honestly. Added May 4, 2026 from S13-T5 AMENDMENT-1. |
| 186 | Picker accent (terracotta) drift from app palette | Design / Polish | S13-T5 AMENDMENT-2 introduced terracotta `#C97B5E` as the unified picker accent for both selected wedges and Start button — replacing per-focus-type purple `#534AB7` (body) and green `#1D9E75` (state). The rest of the app continues to use the body-purple / state-green color logic on the home pie wedges, session card chrome, level-up celebrations, etc. The picker is the only surface where this body/state color distinction collapses. **Why it's there:** intentional per AMENDMENT-2 — the picker is a transient overlay where the user already knows what focus they tapped (title says it). Color identity isn't doing semantic work in the picker the way it does on the home pie. **Future audit option:** propagate terracotta to other transient/contextual surfaces (sheets, modals, overlays) so the app develops a third accent role: purple = body identity, green = state identity, terracotta = transient action. OR revisit the picker accent if signups surface user feedback that the color shift between home pie (purple body wedge) → picker (terracotta selection) → session card (purple body chrome) feels disjointed. Not a bug. Worth a future visual-system audit. Added May 4, 2026 from S13-T5 AMENDMENT-2. |
| 187 | Five-phase orchestrator (Flutter) | Flutter / Approach 5 | Embedded player mode for strength/yoga/breathwork, phase state machine, pause/resume across phases. Sprint-10's React 5-phase orchestrator never made it across the Flutter rebuild (Sprints 7–10 shipped pillar players standalone; the 5-phase mode was deferred). S13-T6 pre-flight diagnostic (May 6, 2026) confirmed the gap is sprint-sized, not ticket-sized: needs a `FivePhaseSessionPage`, phase indicator UI, cross-phase pause/resume, plus an embedding refactor across the three pillar players. **Will land across S14-T3 (orchestrator skeleton + stubbed embedded players) and S14-T4 (player embedding refactor — strength/yoga/breathwork gain embedded mode).** Added May 6, 2026 from S13-T6 rescope. |
| 188 | State-focus 3-leg chain (Flutter) | Flutter / Approach 5 | Multi-technique navigation for state-focus sessions, silent-timer page for null-`content_id` reflection legs, skip/back leg UX, breathwork completion handlers that advance the leg manager. State-focus sessions emit a `legs[]` shape that S13's home page surfaces as a single card; the actual leg-by-leg playback is unbuilt. S13-T6 pre-flight diagnostic (May 6, 2026) confirmed leg navigation is its own discrete build, distinct from the strength/yoga handoff path. **Will land in S14-T5.** Added May 6, 2026 from S13-T6 rescope. |
| 189 | Strength `startFromList` provider method + server endpoint | Flutter + backend / Approach 5 | Ad-hoc strength sessions (no `workout_id`) — the engine emits a flat `items[]` for body-focus strength_only suggestions, but `WorkoutSessionProvider.startSession` currently requires a saved-routine `workout_id` round-trip. Need a new server endpoint accepting a list of exercises directly + a client-side provider method that bypasses the routine table. S13-T6 pre-flight diagnostic surfaced this as Anomaly #1 — strength is the only pillar that can't accept engine output without a stored-routine indirection. **Will land in S14-T1.** Added May 6, 2026 from S13-T6 rescope. |
| 190 | Yoga engine adapter (Flutter) | Flutter / Approach 5 | Phase-name remap (engine `main` → yoga player `peak`), `holdSeconds` conversion (engine emits `duration_min` per pose, yoga player expects per-pose `holdSeconds`), `content_id` → `id` rename, defaults for missing fields the engine doesn't emit (style, breathing cues per pose). The yoga player was built for hand-curated routines and doesn't accept engine output as-is. S13-T6 pre-flight diagnostic (May 6, 2026) catalogued the four shape divergences. **Will land in S14-T2.** Added May 6, 2026 from S13-T6 rescope. |
| 191 | `SuggestedSession` model strictness — enum-validate `session_shape` and `content_type` | Flutter / Type safety | S13-T3's `SuggestedSession` parser accepts any string for `session_shape` (e.g. `'strength_only'`, `'cross_pillar'`, `'state_focus'`) and `content_type` (e.g. `'strength'`, `'yoga'`, `'breathwork'`) — strings are forwarded to consumers without enum validation against the engine's known inventory. Engine-side dictionaries are stable but the Flutter parser would silently pass through a typo or new shape rather than throwing at parse time. **Track-with S14-T1** so the launcher can switch on a closed enum exhaustively. ~10-line refactor: define `SessionShape` and `ContentType` enums, throw `FormatException` on unknown values in `SuggestedSession.fromJson`. Added May 6, 2026 from S13-T6 pre-flight. |
| 192 | SuggestProvider race in `previewFocus` — read `currentSession.metadata.bracket` not `currentFocusSlug` | Flutter / Race condition | Anomaly #10 from S13-T6 pre-flight diagnostic (May 6, 2026). `SuggestProvider.previewFocus(slug)` reads `currentFocusSlug` to decide whether to revert on dismiss, but `currentFocusSlug` is the visual-only seam — the *actual* engine input that produced `currentSession` lives in `currentSession.metadata` (which includes the bracket for state-focus). If the user taps focus A, sees session A, taps focus B (preview), then dismisses without selecting, the revert reads `currentFocusSlug` which may have been mutated mid-preview. Should read `currentSession.metadata.bracket` (or equivalent stable handle) instead. **Track-with S14-T1** since the launcher's pre-Start state read needs the same correctness. Not biting today because the home page only mutates `currentFocusSlug` on confirm, not on hover-style preview — but the API surface invites the bug. Added May 6, 2026 from S13-T6 pre-flight. |
| 193 | Yoga swap-from-engine fallback — `practice_type` style absent from engine output | Flutter / Yoga | Engine emits yoga sessions without a `practice_type` style field — `YogaSession.type` (`'hatha'` / `'vinyasa'` / etc.) is what `loadAlternatives` uses to filter swap candidates. When a yoga session originates from the engine (not a hand-curated routine), the swap sheet has no style anchor and falls back to global candidates, which can surface poses that don't match the session's flow. Two fix paths: (a) disable swap on engine-originated yoga sessions, or (b) hardcode a default style per yoga `session_shape` (e.g. `peak_flow` → `'vinyasa'`, `restorative` → `'yin'`). **Track-with S14-T2** since the yoga adapter is the right place to attach a default style. Added May 6, 2026 from S13-T6 pre-flight. |
| 194 | `exercises.is_active` column — verify intent: dropped or never existed | Schema / Audit | Surfaced from S14-T1 build (May 7, 2026). Original spec §4.1 referenced an `is_active = false` filter on `exercises` lookups; pre-flight against live DB found no such column. T1 dropped the filter (existence check via JOIN is functionally equivalent for active T1 use cases). Worth a one-pass schema audit to confirm: was this column ever shipped and later dropped? Is the `exercises` table missing a soft-delete mechanism that some upstream code assumes exists? Two paths: (a) confirm via `git log -p server/src/db/migrate.js` that no `is_active` migration ever ran — close as doc-only confirmation; (b) if a soft-delete pattern is genuinely wanted (for retiring exercises without losing historical session refs), add the column + backfill `true` for all rows + audit query sites. **Trigger:** any future ticket that wants soft-delete on exercises (e.g. retiring deprecated movements). Don't pull off the shelf as a standalone ticket. Added May 7, 2026. |
| 195 | `exercises.equipment` column — same situation as `is_active` | Schema / Audit | Surfaced from S14-T1 build (May 7, 2026). T1 spec §4.1 included `e.equipment` in the JOIN response shape; live `exercises` table has no `equipment` column. T1 dropped it from the JOIN. Player code reads `exercise['equipment'] as String? ?? ''` so the empty fallback hides the absence. **Already covered by FUTURE_SCOPE #141** (Equipment column + filtering on `exercises` table) — this entry exists only to cross-reference: when #141 lands, T1's `start-from-list` endpoint should add `equipment` back to the JOIN response. Closes when #141 closes. Added May 7, 2026. |
| 196 | `ApiConfig.baseUrl` audit — endpoint constants must NOT have `/api` prefix | Code hygiene / Convention lock | **RESOLVED S15-T1 (May 15, 2026).** The `/api` convention is now the documented standard at `docs/ARCHITECTURE.md` §4.3 and §4.6, and a convention comment lives at the top of `app/lib/config/api_config.dart` (post-refactor). The sweep found zero pre-existing offenders among the endpoint constants. Original entry follows for history. — Surfaced from S14-T1 build (May 7, 2026). T1's reroute prompt initially specified `sessionsStartFromList = '/api/sessions/start-from-list'`, which would produce `/api/api/sessions/...` because `ApiConfig.baseUrl` already ends in `/api`. Claude Code caught this at pre-flight, dropped the prefix to `/sessions/start-from-list`, matched repo convention. **Followup:** add a one-line comment at the top of `app/lib/config/api_config.dart` documenting the convention so future tickets don't repeat the trip-up. Sweep the file for any existing constants that mistakenly include `/api` and clean them up. Likely zero pre-existing offenders — repo convention has been consistent — but worth confirming. **Trigger:** next polish/refactor sprint that touches `api_config.dart`. ~10-line cleanup. Added May 7, 2026. |
| 197 | Engine `state_focus` recipe rejects bracket value `'10_to_20'` with `RangeError: invalid bracket value` | Engine bug / T5 prerequisite | Surfaced from S14-T1 verification script (May 7, 2026). Throwaway `verify-s14-t1-shapes.mjs` ran the engine against `(entry_point='home', focus='energize', time_budget_min=30, bracket='10_to_20')` to confirm shape inventory; engine threw `invalid bracket value: 10_to_20`. The bracket token format the engine expects differs from what the verify script guessed. Either the engine's accepted bracket vocabulary is `'short' / 'medium' / 'long'` style (not range-string style), or the script picked a token that's no longer valid. Not a T1 issue (T1 doesn't ship state_focus). **Critical for T5 (state-focus 3-leg chain):** before T5 spec authoring, run a focused pre-flight that grep's the engine for accepted bracket literals and confirms which strings the state-focus recipe accepts as input. The S13-T5 bracket-picker UI already produces some bracket value — verify what string the picker emits and that the engine accepts it. **Trigger:** start of T5 spec authoring (Sprint 14, after T4 ships). Don't chase before then; the bug doesn't block T1-T4. Added May 7, 2026. |
| 198 | Engine cross_pillar phase-substitution fallback | Engine / Approach 5 | S14-T2 pre-flight (May 8, 2026): biceps emits 4-phase cross_pillar at 30 and 60 min — cooldown dropped because muscle-specific yoga pool is empty after warmup exclusions. Engine handles gracefully; T2 orchestrator iterates `phases.length` so it runs end-to-end. **Problem:** if we brand home as "5-phase," a 4-phase session is a leaky abstraction. **Fix:** engine substitution ladder when a phase pool empties — adjacent muscle → generic restorative → allow warmup duplicate. ~30–50 LOC in `suggestionEngine.js` + smoke assertion that cross_pillar always emits 5 phases. Likely also affects triceps / calves. **Trigger:** before signups open. Natural home is S14-T6 (polish + edge cases). Priority: high. |
| 199 | Engine emits savasana phase for yoga sessions | Engine / Content | S14-T3 ships the yoga adapter without a savasana phase because the engine emits only `warmup → main → cooldown` for `yoga_tab` sessions. Traditional yoga ends with savasana (corpse pose, ~5% of session duration). The T3 adapter's phase remap (`_enginePhaseToYogaPhase`) only knows `warmup/main/cooldown` and throws `StateError('unknown yoga phase token: savasana')` if the engine ever emits one — which is the right strict-mode failure. Fix at the engine recipe level (`server/src/services/suggestionEngine.js` yoga-only recipe), not at the T3 adapter. When engine ships savasana emission, add `'savasana': 'savasana'` to the adapter's phase map (the existing yoga player already supports `'savasana'` as a phase token — see `yoga_session_page.dart:13-31` `_phaseColors`/`_phaseEmojis`/`_phaseLabels`). Companion to FS #200. **Trigger:** Sprint 14+ engine work. Surfaced from S14-T3 build (May 10, 2026). |
| 200 | Engine emits `metadata.yoga_style` for yoga sessions | Engine / Content | S14-T3 adapter hard-defaults `YogaSession.type = 'vinyasa'` (locked in spec §4.3) because the engine doesn't carry a style label in `metadata`. Consequence today: when the user taps "swap pose" mid-session, the alternatives query at `/api/yoga/alternatives` filters by `practiceType = session.type = 'vinyasa'` even when the engine selected (say) hatha-leaning poses. Documented limitation; doesn't crash, just returns suboptimal alternatives. **Fix:** engine reads pillar level + duration + pose mix → emits `metadata.yoga_style` in {vinyasa, hatha, yin, restorative, sun_salutation}. Adapter then reads `session.metadata.yogaStyle ?? 'vinyasa'` instead of hardcoding. Companion to FS #199 (best done together — both are engine yoga-recipe enrichment). **Trigger:** Sprint 14+ engine work. Surfaced from S14-T3 build (May 10, 2026). |
| 201 | Audit yoga library for body-focus orphans (companion to FS #142) | Content / Approach 5 | S14-T3 PF1 confirmed engine emits clean `pillar_pure` shape for `yoga_tab/(hamstrings\|back\|mobility\|full_body\|biceps)/30min`. But `hips` is **not a seeded body focus slug** — engine throws `Unknown or inactive focus_slug: hips` for all 4 `hips` tuples, even though `hips` is a meaningful yoga concept and many poses target hip mobility. The 12 seeded body focuses (S11-T3) miss `hips` as a first-class option. Same class of issue as FS #142 (yoga catalog coverage gap for body focuses): yoga poses that genuinely target a body region but aren't visible to the engine because of (a) missing keyword in `target_muscles`, (b) missing `vinyasa` in `practice_types`, or (c) <3 muscles for the full_body fallback rule. Hand-audit + re-tag is the fix. Could surface 50+ poses currently invisible to `yoga_tab` queries across hip-openers, twists, balance poses. **Trigger:** Sprint 13+ content sprint. Adds `hips` as a 13th body focus once content is tagged. Surfaced from S14-T3 PF1 (May 10, 2026). |
| 202 | Yoga tab cold-start default `'hamstrings'` is unreachable dead code | Code hygiene / UX | S14-T3 spec locked `'hamstrings'` as the Yoga tab cold-start fallback focus (`_kYogaTabFallbackFocus` in `app/lib/pages/yoga/yoga_page.dart:20`). Used at two sites: `didChangeDependencies` and the `_TodaysYogaCard.onRetry` callback. **Problem:** `SuggestProvider._currentFocusSlug` defaults to `'full_body'` (`app/lib/providers/suggest_provider.dart:10`), and `hydrate()` overwrites with whichever slug was previously persisted. Either way, `currentFocusSlug` is never `''` after first home-page mount — so the `isNotEmpty ? currentFocusSlug : 'hamstrings'` ternary always picks `currentFocusSlug`. The Yoga tab inherits home's focus, never falls back to `'hamstrings'`. **Two fix paths:** (a) **Remove the fallback** — accept that the Yoga tab inherits home's focus deterministically, simplify the ternary to `await suggest.refreshForEntryPoint(entryPoint: 'yoga_tab', focusSlug: suggest.currentFocusSlug)`. Document that home's default seeds the Yoga tab cold-start. (b) **Decouple**: change `SuggestProvider` to leave `currentFocusSlug = ''` on cold start and let each tab supply its own default (Strength tab → `'biceps'`, Yoga tab → `'hamstrings'`, Home → `'full_body'`). Cleaner separation but more churn. (a) is the pragmatic call. **Trigger:** Sprint 14 polish (T6) or whenever the Yoga tab UX is revisited. Surfaced from S14-T3 build (May 10, 2026). |
| 203 | T3 yoga adapter polish bundle — W1/W2/W3 + A1/A2/A3 from `/review` | Flutter / Polish | S14-T3 `/review` Grade **B+**, debt **3/10**. 5 polish issues bundled for S14-T6 fix-pass (~30–60 min Flutter work): (W1) **Timer leak when navigation aborts post-hydration** — `loadFromEngineSession` calls `startSession()` which fires `Timer.periodic` BEFORE navigation; if `context.mounted == false` after the await, timer ticks invisibly inside the provider until next session reset. Fix: call `provider.reset()` in launcher's post-await `!mounted` branches. Structural alternative: split `startSession` into `loadSession()` + `beginPlayback()` — affects manual builder flow too, larger change. (W2) **Fragile substring matching in `_friendlyYogaError`** — same anti-pattern as FS #166 (engine `RangeError` text matching), reproduced client-side. Adapter throw text changes silently break user copy. Fix: typed exception hierarchy (`YogaHydrationException` / `YogaContractException`) instead of `StateError` + `msg.contains(...)`. (W3) **Card empty after state→body→yoga roundtrip** — T1 carry-over: `didChangeDependencies` only re-fetches when `_lastFetchedFocusSlug != currentFocus`. After visiting Home with body focus → returning to Yoga tab → focus matches but `currentSession` is now cross_pillar shape from home. Card shape-guard returns `SizedBox.shrink()`. Fix: also re-fetch when `currentSession?.sessionShape != 'pillar_pure'`. Apply same fix to `strength_page.dart:51` (same bug). (A1) **`_capitalizeFocus` triplicated** across `strength_page.dart:566`, `yoga_page.dart:410`, and now any future tab card. Extract to `app/lib/utils/focus_display.dart`. (A2/A3) **File-location cleanup** — `yoga_session_adapter.dart` and `YogaPoseDetails` live in `app/lib/launchers/`; should move to `app/lib/adapters/yoga_session_adapter.dart` + `app/lib/models/yoga_pose_details.dart` so the dependency arrow flows: provider → adapter, launcher → provider, instead of provider → launchers. Pairs naturally with FS #204 (adapter unit tests — easier to test once relocated). **Trigger:** S14-T6 polish ticket. Surfaced from S14-T3 `/review` (May 10, 2026). |
| 204 | Unit tests for the pure yoga adapter | Flutter / Testing | S14-T3 `/review` flagged: the adapter (`yogaSessionFromEngine` in `app/lib/launchers/yoga_session_adapter.dart`) is a pure function — deterministic, no IO, no `BuildContext` — and is the highest-leverage testable surface in T3. The smoke harness covers (a) engine emission shape and (b) endpoint round-trip but NOT (c) the adapter itself. Add `app/test/launchers/yoga_session_adapter_test.dart` covering: phase remap (`main` → `'peak'`); throws on `session_shape != 'pillar_pure'`; throws on missing `metadata.focus_slug`; throws on `unknown yoga phase token`; throws on `content_id == null`; throws on `duration_minutes <= 0`; throws on missing hydration entry; `holdSeconds == duration_minutes × 60`; level fallback to `'beginner'` when `metadata.userLevels.yoga` absent; `YogaSession.type` hard-defaults to `'vinyasa'`; empty phases skipped silently; multi-phase session produces poses in phase-then-item order. ~12 test cases, ~150 LOC. **Bundle with FS #203** so the file relocation (A2 — move adapter out of `launchers/`) and the tests land in the same T6 commit. **Trigger:** S14-T6 polish ticket. Surfaced from S14-T3 `/review` (May 10, 2026). |
| 205 | Breathwork audio (Flutter) | Mobile / Polish | Sprint 9's Flutter breathwork rebuild (Apr 16, 2026) explicitly chose option B: "No audio for now, add later." The React PWA had Web AudioContext-driven sine wave + filtered noise breath sounds (Sprint 4 `S4-T3`) but were not ported. As a result, the Flutter `BreathworkPlayer` (both standalone and embedded inside the S14-T4 cross-pillar orchestrator) is silent — no inhale/hold/exhale audio cues. Implementation options for Flutter: (a) `audioplayers` package + pre-recorded chime/breath MP3s in `app/assets/audio/`; (b) `flutter_tts` for spoken cues ("inhale… hold… exhale…"); (c) `just_audio` + procedurally-generated breath envelopes via PCM buffer. **Recommend (a)** for simplicity — pre-recorded soft chimes at phase transitions match what top breathwork apps (Othership, Breathwrk) do for non-narrated mode. **Trigger:** any breathwork-polish ticket, or a dedicated mobile-polish pass post-Sprint-14. **NOT a S14-T4 regression** — silent state predates T4 by 25 days. Surfaced from S14-T4 device test (May 11, 2026); documented in `Trackers/S14-T4-AMENDMENT-2-device-findings.md` Finding 2. |
| 206 | Recover & Focus breathwork bracket coverage | Data / Engine | The 0-10 min bracket for `recover` and `focus` state focuses emits `state: 'empty'` in the picker because the breathwork tag taxonomy doesn't have enough beginner-eligible techniques tagged for `recover_eligible` and `focus_eligible`. Audit existing 49 breathwork techniques against the eligibility tags — likely 3-5 techniques per focus need re-tagging (preferred), or relax the engine's threshold for these focuses. Affects ~5% of state-focus session attempts (beginner users on recover or focus state focus + short bracket). **Trigger:** Sprint 15+ content sprint or a focused engine threshold tune. Surfaced from S14-T5 device test (May 12, 2026); documented in `Trackers/S14-T5-AMENDMENT-1-device-findings.md` Finding 5. |
| 207 | Profile screen with pillar levels + level progress | UI / Profile | No profile screen exists. Users can't see their pillar levels (beginner/intermediate/advanced), session counts, level-up progress, or any personal stats. Pillar levels drive engine bracket eligibility but are invisible to the user. Build a Profile tab showing: pillar levels (3 cards), level-up progress bars (sessions to next level), recent session history, lifetime stats. Likely a full M ticket on its own (~2-3 days). **Trigger:** Sprint 15 or 16, depending on signups timing. Surfaced from S14-T5 device test (May 12, 2026); documented in `Trackers/S14-T5-AMENDMENT-1-device-findings.md` Finding 6. |
| 208 | Cross-pillar yoga style coherence | Engine / Content | Cross-pillar sessions for a single body focus may emit yoga warmup/cooldown phases drawn from different styles (e.g. warmup in vinyasa, cooldown in hatha) because engine pose selection is style-agnostic per phase. Result: stylistic incoherence within a single session. **Fix:** engine pre-picks a yoga style per session and constrains pose selection across all yoga phases to that style. Pairs with FS #200 (engine emit `metadata.yoga_style`) and FS #199 (savasana). **Trigger:** Sprint 14+ engine yoga-recipe enrichment, paired with FS #199/#200. Surfaced from S14-T6 spec authoring (May 12, 2026). |
| 209 | Neon DB cold-start timeout bump | Engine / Performance | `ApiService._kRequestTimeout = 15s` (`app/lib/services/api_service.dart:9`) is too tight for Neon Postgres cold-start scenarios. First request after idle period regularly takes 8-12s for DB warmup; 15s margin is precarious. When timeout fires, client maps to `NetworkError` → "Check your connection and try again." copy — misleads users into thinking their network is broken when it's a server cold-start. **Fix options:** (a) bump timeout to 30s for `/suggest` and `/start-from-list` endpoints (read-side ops that hit Neon hard); (b) add Neon warming ping at app boot (`GET /api/health`); (c) split timeout: 8s for cached endpoints, 30s for engine endpoints. **Related:** the cold-start gate (FS #224, shipped Commit 2) reduced visibility but didn't eliminate. **Trigger:** before Google Play launch, OR when first user reports "app says no internet but my internet is fine". Surfaced from S14-T6 Commit 2 investigation (May 13, 2026). |
| 210 | Level-up celebration on summary screen — verification deferred | UX / Verification | S14-T6 spec §6.5 added a level-up celebration overlay on the multi-phase completion summary screen when the user crosses a pillar threshold during the session. Implementation landed in Commit 1. Device test could not reproduce a level-up event (test account was already at max level for all 3 pillars). Visual treatment + animation timing untested. **Fix:** during Sprint 15 test pass, manually demote test account pillar levels then complete a session that crosses a threshold. Verify overlay renders, timing feels right (not too fast, not too long), tap-to-dismiss works, summary screen state behind overlay is correct after dismiss. **Trigger:** Sprint 15 device-test pass. Surfaced from S14-T6 device retest #1 (May 12, 2026). |
| 211 | Recency banner "Switch to Recover" modality mismatch — needs design discussion | UX / Product | The recency-overlap warning banner (S14-T6 §6.4) emits `alternative_focus_slug: 'recover'` when the user trained the same muscle yesterday. Tapping "Switch to Recover" routes through `selectStateFocus(bracket)` since 'recover' is a state focus. **Problem:** the user is on a body-focus card surface (Home / Yoga tab / Strength tab) when they tap the banner — switching modalities to a breathwork-based state-focus session is a context jump. UX research call: should the "alternative" be a different body focus (e.g. complementary muscle) instead of a modality switch? Or should the banner surface the modality switch more explicitly ("Today's body work overlaps yesterday's — want to try a recovery breath session instead?"). **Trigger:** Sprint 15 product/UX design discussion. Surfaced from S14-T6 Commit 1.5 device retest (May 12, 2026). |
| 212 | Unified sessions view (Postgres VIEW `v_completed_sessions`) | DB / Architecture | Three tables hold completed-session records: `sessions` (strength via `start-from-list`), `breathwork_sessions`, `yoga_sessions`. Cross-pillar sessions write to two of these per request via the FK linkage on `multi_phase_sessions`. Querying "all sessions for user X" requires three UNION ALLs with column-aliasing — boilerplate that's already duplicated in `home/stats`, `home/daily-counts`, `home/weekly-activity`, `analytics/calendar`. **Fix:** create a Postgres VIEW `v_completed_sessions` that UNIONs the three with normalized columns (`user_id, pillar, completed_at, duration_min, focus_slug, multi_phase_session_id`). Endpoints query the VIEW. **Trigger:** Sprint 15+ when next endpoint needs the union, OR when first noticeable drift between the three duplicated queries surfaces. Surfaced from S14-T6 `/review` pass 1 (May 12, 2026). **[ChatGPT review, May 15, 2026]** Also independently surfaced by the ChatGPT code review — two independent reviewers reached the same conclusion, raising priority signal. See `Trackers/CHATGPT_REVIEW_TRACEABILITY.md` F7 entry (merged into this row, May 15, 2026). |
| 213 | SessionEndCoordinator — centralize navigation + teardown | Flutter / Architecture | Session-end logic is scattered across `multi_phase_session_page.dart`, `yoga_session_page.dart`, `workout_page.dart`, `breathwork_timer_page.dart`. Each page does: (a) log session to API, (b) clear provider state, (c) maybe show summary, (d) navigate to /home (or stay on summary). Inconsistencies exist (e.g. PopScope handling, double-log guards). **Fix:** extract `SessionEndCoordinator` that takes (BuildContext, SessionResult, Provider) and runs the canonical end-of-session flow. **Trigger:** Sprint 15+ refactor pass, OR when next session-end bug surfaces. Surfaced from S14-T6 `/review` pass 1 (May 12, 2026). |
| 214 | Type-safe `go_router` extra parameters | Flutter / Type safety | Routes pass `state.extra` as `dynamic` and downstream code casts with `as Map<String, dynamic>?`. Examples: `/workout` (extra is `{workoutId, exercises}`), `/session/summary` (extra is `SessionSummaryArgs`), `/workout/resume` (extra is `{resumeData}`). The session-summary case is type-safe (uses `is! SessionSummaryArgs` guard + bounce); the others are not. **Fix:** introduce a sealed `RouteExtra` hierarchy and constrain `extra:` to instances of it. **Trigger:** Sprint 15+ Flutter refactor, OR when first `extra`-cast crash surfaces. Surfaced from S14-T6 `/review` pass 1 (May 12, 2026). |
| 215 | Coerce `req.user.id` to int at auth middleware boundary | Backend / Type safety | Multiple route handlers cast `req.user.id` to int with `parseInt(req.user.id, 10)` or rely on implicit coercion in SQL queries. The JWT verify middleware (`server/src/middleware/auth.js`) decodes the payload but doesn't enforce `id` is int — depends on token issuance not drifting. **Fix:** in the middleware, after decoding, validate + coerce: `req.user.id = Number.isInteger(decoded.id) ? decoded.id : parseInt(decoded.id, 10); if (isNaN(req.user.id)) return res.status(401).json({error:'invalid_token'});`. Then remove the per-route coercions. **Trigger:** Sprint 15+ backend hardening sweep. Surfaced from S14-T6 `/review` pass 1 (May 12, 2026). ✅ **SHIPPED in S15-T7 (`0a35a9a`, May 18, 2026).** Implementation deviated from the proposal: dropped the `parseInt` fallback and used bare `Number.isInteger(decoded.id) || decoded.id <= 0` instead — coercing string-digit ids would have silently widened the trust boundary. Only `workout.js` had an in-scope coercion to remove. |
| 216 | Stop using `try/catch (e)` in Dart — narrow to `on Exception catch (e, stack)` | Flutter / Code hygiene | Bare `catch (e)` in Dart catches `Error` subclasses (AssertionError, StateError, ArgumentError) along with `Exception` subclasses. Programmer bugs that should crash loud get silently swallowed and re-presented as runtime errors. Multiple call sites use this pattern across providers and services. **Fix:** sweep `git grep "catch (e)" -- 'app/lib/**/*.dart'`, narrow each to `on Exception catch (e, stack)` unless the call site genuinely needs to catch Errors. Document rule in CONTRIBUTING.md. **Companion to FS #234** (specific to `loadFromEngineSession`). **Trigger:** Sprint 15+ code-hygiene pass. Surfaced from S14-T6 `/review` pass 1 (May 12, 2026). |
| 217 | Smoke harness deterministic counts OR stable-fail-only reporting | DevOps / Test harness | `server/scripts/test-suggestion-engine-t2.js` reports pass/fail counts that drift between runs due to fixture creation order, parallel database state, and the body-focus matrix flake. T6 commit messages had to qualify smoke totals across passes (3471 → 3499 → 3534 — none of which are "the" baseline). **Fix options:** (a) make pass counts strictly deterministic across runs (clean DB state per run, serialize all fixture creation); (b) accept count drift but report only stable fails — i.e., a fail that reproduced across 3 consecutive runs. Either gives commit messages a defensible "no new fails" claim. **Trigger:** Sprint 15+ DevOps pass. Surfaced from S14-T6 `/review` pass 2 (May 12, 2026). |
| 218 | Transaction-wrapped smoke sub-blocks | DevOps / Test harness | Smoke sub-blocks (e.g. T6 SWAP-EXCLUSION) don't use BEGIN/ROLLBACK boundaries — they write fixtures, assert, then clean up with explicit DELETE. If a sub-block throws mid-execution, fixtures leak. **Fix:** wrap each sub-block in `BEGIN ... ROLLBACK` (or `BEGIN ... ROLLBACK` even on success — assertions are read-only against the in-transaction state). This guarantees zero fixture leak. **Trigger:** Sprint 15+ when refactoring smoke harness for #217. Surfaced from S14-T6 `/review` pass 2 (May 12, 2026). |
| 219 | Streak endpoint: HTTP 400 → 500 for invalid user_id | Backend / API hygiene | The streak endpoint returns 400 for invalid `user_id`, which is wrong — `user_id` comes from JWT, not user input, so an invalid value is a server bug (corrupted token, decode mismatch), not a client error. Should be 500. **Fix:** one-line in the route handler. Audit other endpoints for the same anti-pattern. **Trigger:** Sprint 15+ backend hardening. Surfaced from S14-T6 `/review` pass 3 (May 12, 2026). **[2026-05-15 verification note]** Re-read `server/src/routes/users.js:143-252` while documenting `docs/API.md`. The streaks handler has no 400 path — invalid user_id flows directly to SQL and any failure routes through `errorHandler.js` as 500. FS #219's claim that the endpoint returns 400 does not match the live code. Either this FS entry is stale (was true once, code changed) or was inaccurate from the start. Needs investigation before being actioned — the "bug" it describes may not exist. |
| 220 | Multi-row smoke fixture: incremental ID accumulation | DevOps / Test harness | `assertSession` helper creates fixture session rows. Subsequent assertions reference the IDs of those rows via accumulator variables. Pattern is fragile — adding an assertion in the middle shifts indexes downstream. **Fix:** assign each fixture a stable named key, reference by key throughout. **Trigger:** Sprint 15+ smoke harness refactor (paired with #217/#218). Surfaced from S14-T6 `/review` pass 3 (May 12, 2026). |
| 221 | Drop redundant `SQL DISTINCT` in ordered CTE | Backend / SQL hygiene | Several home-page aggregation queries use `SELECT DISTINCT ...` inside a CTE that is already grouped or ordered. The DISTINCT is a no-op but adds a sort pass. **Fix:** audit queries in `server/src/services/`, drop redundant DISTINCT where GROUP BY or ORDER BY already establishes uniqueness. **Trigger:** Sprint 15+ SQL hygiene pass. Surfaced from S14-T6 `/review` pass 3 (May 12, 2026). |
| 222 | Add explanatory comment for `is_first: row.total <= 1` boundary | Backend / Code hygiene | The level-up trigger uses `is_first: row.total <= 1` to detect a user's first-ever session of a kind. The `<= 1` (vs `=== 0`) handles the race where the current session has already been inserted before the count read. Boundary is subtle; a future engineer might "fix" it to `=== 0` and break the trigger. **Fix:** add a comment naming the race. **Trigger:** Sprint 15+ code hygiene pass. Surfaced from S14-T6 `/review` pass 3 (May 12, 2026). |
| 223 | PopScope on session summary screen | Flutter / UX | The multi-phase session summary screen (post-session) doesn't intercept hardware back. User can back-navigate out of summary into the now-disposed session page, hitting the bounce-home path. **Fix:** wrap summary in `PopScope(canPop: false, onPopInvoked: ...)` that routes back to /home directly. **Trigger:** Sprint 15+ UX polish. Surfaced from S14-T6 `/review` pass 3 (May 12, 2026). |
| 224 | Yoga tab cold-start empty state | Flutter / UX | ✅ **SHIPPED in Commit 2 (`c47a1a0`, May 13, 2026).** Closed-traceability entry. Cold-start with no focus selected on home surfaced a misleading "Check your connection and try again" error card on Yoga tab. Root cause: `didChangeDependencies` fired a fetch with the provider's default slug ('full_body'), engine took >15s on Neon cold start, timeout mapped to `NetworkError`. **Fix shipped:** `SuggestProvider.hasUserSelectedFocus` flag gates the Yoga tab + Strength tab auto-fetch in `didChangeDependencies`; new `_YogaTabEmptyState` / `_StrengthTabEmptyState` widgets render in the today's-session slot when no user pick has been confirmed. Flag set true via `_runRequest` (covers selectBodyFocus/selectStateFocus/refreshForEntryPoint) and `hydrate()` (when a non-empty stored slug restores). Reset to false on `clear()` (logout). `previewFocus` does NOT flip — picker hover is a visual seam, not a confirmed choice. Surfaced from S14-T6 Commit 2 device test (May 13, 2026). |
| 225 | Yoga tab state-focus redirect hint | Flutter / UX | When the user has selected a state focus (Calm/Energize/Focus/Sleep/Recover) on Home and opens the Yoga tab, the today's-session card collapses to `SizedBox.shrink()` with no explanation. User has no breadcrumb pointing them at Home for their session. **Fix:** render a small "State focus selected — see Home for today's session" hint card with a CTA to navigate Home. **Trigger:** Sprint 15+ UX polish. Surfaced from S14-T6 Commit 2 (May 13, 2026). |
| 226 | State-focus constants single source of truth | Flutter / Architecture | ✅ **SHIPPED in Commit 2 (`c47a1a0`, May 13, 2026).** Closed-traceability entry. Three sites duplicated the `_kStateFocusSlugs` set: `launchers/focus_utils.dart`, `widgets/home/entry_point_warning_slot.dart`, and the new yoga_page logic. **Fix shipped:** new `app/lib/constants/focus_categories.dart` exports `kStateFocusSlugs` set + `isStateFocus(slug)` helper as the single source of truth. `launchers/focus_utils.dart` deleted; `entry_point_warning_slot.dart` updated to import from constants/. All consumers (yoga_page, strength_page, entry_point_warning_slot) now import from one place. Surfaced from S14-T6 Commit 2 build (May 13, 2026). |
| 227 | Race tracker: gate `didChangeDependencies` on `!isLoading` | Flutter / Concurrency | `SuggestProvider`'s race tracker keys on slug alone. FS #224's gate fix reduced the race surface but didn't eliminate it. Scenario: user picks biceps → fetch fires → user instantly opens Yoga tab → second fetch fires for same slug with different entry_point. Race tracker treats them as the same request; last writer wins. If home response arrives last (cross_pillar shape), Yoga card renders nothing (cross_pillar ≠ pillar_pure). **Fix:** add `entry_point` alongside slug in the race tracker key, OR gate `didChangeDependencies` on `!suggest.isLoading` so the second fetch waits. Pairs with FS #230. **Trigger:** Sprint 15+ concurrency pass, OR when first race-related bug surfaces in production. Surfaced from S14-T6 `/review` on Commit 2 W-2 (May 14, 2026). |
| 228 | `PillarSessionException` sealed parent hierarchy | Flutter / Architecture | Yoga uses typed exceptions (`YogaSessionException` sealed class with `YogaContractException`/`YogaHydrationException` subtypes). Strength uses error-code string matching (`_friendlyError` in `session_launcher.dart`). Breathwork has no error abstraction yet. Three paradigms emerging — inconsistency is a smell. **Fix:** `PillarSessionException` sealed parent with `YogaSessionException`/`StrengthSessionException`/`BreathworkSessionException` subclasses. Each pillar's typed subclasses live underneath. Launcher's catch ladder unifies on the parent. Pairs with FS #237 (Dart 3 exhaustive switch). **Trigger:** Sprint 16+ when breathwork pillar gets its own error surface, OR when launcher complexity outweighs the refactor cost. Surfaced from S14-T6 `/review` on Commit 2 architecture rec (May 14, 2026). |
| 229 | Document hot-reload-on-`ChangeNotifier` hazard in `CONTRIBUTING.md` | Docs / Onboarding | Sprint 14 lost 2 hours to hot-reload state pollution during Commit 2 device retest. Adding `bool _hasUserSelectedFocus = false` to long-lived `SuggestProvider` instance produced ghost bug after Calm→Biceps focus transition — flag appeared `false` despite `_runRequest` having set it `true`. `flutter clean + flutter run` resolved instantly. The app has 8+ `ChangeNotifier` providers; this will bite again. **Fix:** add a "Development gotchas" section to `CONTRIBUTING.md` (or top-level `README.md`) naming the hazard: when adding a new field (especially nullable/bool) to a long-lived `ChangeNotifier` provider, `flutter clean + flutter run` before device-testing. Hot reload preserves the existing instance and may not initialize the new field. **Trigger:** Sprint 15 first ticket, OR when a second developer joins. Surfaced from S14-T6 Commit 2 retest debug detour (May 13, 2026). |
| 230 | Track `entry_point` alongside slug in race keys | Flutter / Concurrency | Related to #227 but broader scope. Currently `SuggestProvider` tracks the in-flight request by slug only. Adding `entry_point` ('home', 'yoga_tab', 'strength_tab') alongside slug in the race tracker key would unlock safer concurrent fetches across tabs and let us remove the W3 `sessionShape != pillar_pure` workaround in `didChangeDependencies` (which is essentially "guess that the slot has stale data"). **Trigger:** Sprint 15+ concurrency pass, paired with #227 if done together. Surfaced from S14-T6 `/review` on Commit 2 architecture rec (May 14, 2026). |
| 231 | Player-level typed exceptions (`yoga_session_player.dart`) | Flutter / Code hygiene | Adapter scope was completed in Commits 2/2.1 (FS #203 W2 + Commit 2.1 CR-1). But `app/lib/players/yoga_session_player.dart:147,151,169,172` still has 4 raw `StateError` throws — same anti-pattern that W2 fixed in the adapter. The player and adapter share failure-mode shape ("missing pose hydration", "no items", "non-positive duration") so the typed exception hierarchy applies. **Fix:** fold into FS #228 (PillarSessionException unification) since the player's failure modes overlap with adapter's. Player throws should use the same typed parent. **Trigger:** Sprint 16+, paired with FS #228. Surfaced from S14-T6 `/review` on Commit 2 (May 14, 2026). |
| 232 | Cross-modality duration semantics — "60 min budget" means what? | UX / Product | When the user selects "60 min" on home for a body focus (e.g. Triceps), the three pillar surfaces produce different durations: Home (cross-pillar) **67 min** — engine assembles all 5 phases into a routine that slightly overshoots the budget to hit whole-pose-count units; Strength tab **60 min** — pillar slice fits exactly into budget; Yoga tab **49 min** — pillar slice fits 13 poses into a 60m envelope; undershoots due to discrete pose durations. This is correct per-modality behavior but the UX is confusing — user picked "60 min" and sees three different numbers. Product question for Sprint 15: should the budget mean *total practitioner time* across all surfaces (normalize), or *target ceiling per modality* (current)? The 30-min test showed perfect parity (Home/Strength/Yoga all 30 min), so the issue only manifests at the discrete-unit boundary (i.e., 60m). May be cosmetic for small budgets and load-bearing only for larger ones. **Trigger:** Sprint 15 product/UX design discussion, OR when first user feedback comes in about duration confusion. Surfaced from S14-T6 Commit 2.1 device retest (May 14, 2026). |
| 233 | `developer.log` alongside `debugPrint` for release-build observability | Flutter / Observability | `resolveSessionStyle`'s unknown-style fallback uses `debugPrint` to log when the engine emits an unknown style. `debugPrint` is a no-op in release builds, so unknown styles silently propagate in production. The reviewer noted this is the third instance of "we should log this for ops but only have `debugPrint`" — a release-day liability. **Proposal:** establish a project-wide observability convention. Options: (a) `dart:developer log()` as the project standard (free, structured, survives release builds, no SDK dependency); (b) Sentry/Firebase Crashlytics (SDK + setup, but production-grade). Update all current `debugPrint`-for-ops calls (3+ instances surfaced) once the standard is adopted. **Trigger:** Sprint 15 observability pass, OR pre-Google-Play submission. Surfaced from S14-T6 `/review` on Commit 2.1 W-2.1.2 (May 14, 2026). |
| 234 | Narrow bare `catch (e, stack)` to `on Exception catch` in `loadFromEngineSession` | Flutter / Code hygiene | `loadFromEngineSession` (post-Commit-2.1 CR-1) uses a bare `catch (e, stack)` to wrap untyped errors in `YogaHydrationException`. Bare catch also swallows `Error` subclasses (assertion failures, type errors) — programmer bugs that should crash loudly in debug builds. **Fix:** narrow `catch (e, stack)` to `on Exception catch (e, stack)`. Programmer bugs (`Error` subclasses) propagate up and crash; runtime errors (`Exception` subclasses) get wrapped. "Fail-fast for programmer errors, fail-friendly for runtime errors." Companion to FS #216 (project-wide sweep). **Trigger:** Sprint 15 code-hygiene pass. Surfaced from S14-T6 `/review` on Commit 2.1 S-2.1.4 (May 14, 2026). |
| 235 | Use `Error.throwWithStackTrace` OR include `stackTrace` in `toString()` | Flutter / Debugging | Commit 2.1 added `cause` and `stackTrace` as optional named params on the sealed `YogaSessionException` parent. The fields are stored but not surfaced — `toString()` includes `cause` but not `stackTrace`, and the launcher's catch handler logs only `userMessage`. **Fix:** either (a) use `Error.throwWithStackTrace(exception, stackTrace)` when re-throwing so Dart's runtime preserves the chain (Dart 2.16+); or (b) override `toString()` on each subclass to include `cause` and `stackTrace.toString().take(500 chars)` for debug builds. Without this, the fields are speculative-but-cheap surface that hasn't earned its place yet (reviewer's exact words). **Trigger:** Sprint 15 code-hygiene pass. Surfaced from S14-T6 `/review` on Commit 2.1 S-2.1.1 (May 14, 2026). |
| 236 | Three-site client/server enum drift hazard | Architecture / Risk | Three constants exist in client code that mirror server-side enums: `kStateFocusSlugs` (5 elements — calm/energize/focus/sleep/recover); `_kKnownYogaStyles` (5 elements — vinyasa/hatha/yin/restorative/power); `_friendlyError` substring matchers in `session_launcher.dart` (legacy, partially deprecated by W2 typed exceptions). When the server adds a new state focus (e.g. gratitude) or new yoga style (e.g. power_hatha), the client silently mishandles it until the constant is updated. **Proposal:** generate client constants from server schema at build time (codegen from `backend seeds/*.ts`), OR add a `/api/health-check` endpoint that returns the server enum manifest for the client to validate against at startup. **Trigger:** Sprint 15+, OR when the first enum-drift bug surfaces in production. Surfaced from S14-T6 `/review` on Commit 2.1 fix #5 (May 14, 2026). |
| 237 | Exhaustive switch on sealed exception hierarchies (Dart 3) | Flutter / Type safety | The launcher's catch ladder currently uses `on Type` chains. Dart 3 supports exhaustive `switch (e) { case YogaContractException(): ... case YogaHydrationException(): ... }` with compile-time exhaustiveness checking on sealed hierarchies. **Proposal:** when FS #228 (PillarSessionException unification) lands, refactor all launcher catch ladders to use Dart 3 sealed switches. Compile-time guarantee that every exception subtype is handled. **Trigger:** Sprint 16+, paired with FS #228. Surfaced from S14-T6 `/review` on Commit 2.1 architecture rec (May 14, 2026). |
| 238 | Integration test for adapter → launcher → snackbar chain | Flutter / Test infrastructure | Unit tests in `yoga_session_adapter_test.dart` (FS #204) lock the adapter's throw behavior. Launcher's catch ladder is tested indirectly via device retest. No automated test verifies that an adapter throw actually surfaces the correct snackbar copy to the user. The W-2.1.1 regression test locks the `userMessage` contract on the exception side, but not the launcher-rendering side. **Proposal:** widget test that pumps a Scaffold containing the launcher dispatch, triggers a yoga session launch with a bad `SuggestedSession`, and asserts the snackbar contains `userMessage`. Best paired with FS #228 (PillarSessionException unification) — when the launcher catch ladder becomes a sealed `switch (e)`, the integration test surface stabilizes and becomes worth the test investment. **Trigger:** Sprint 15+ when adding similar tests for cross_pillar / state_focus launchers, OR when first copy-related regression surfaces in production. Surfaced from S14-T6 `/review` on Commit 3 (May 14, 2026). |
| 239 | Yoga adapter test: cover `session.phases.isEmpty` and "every phase has empty items" guards | Flutter / Test coverage | FS #204 lands 16 adapter tests (12 contract + 3 resolveSessionStyle + 1 W-2.1.1 regression) but misses two adapter throw paths: `session.phases.isEmpty` (line 36–38 of yoga_session_adapter.dart, throws "yoga session has no phases") and the post-loop `poses.isEmpty` guard (line 88–90, throws "yoga session has no poses" when every phase has empty items). Reviewer estimated ~10 lines of test code. **Fix:** add two cases — one fed `phases: []`, one fed three phases all with `items: []`. Assert `YogaContractException` with `.message` `contains('phases')` / `contains('no poses')` respectively. **Trigger:** Sprint 15 test-hygiene pass. Surfaced from S14-T6 `/review` on Commit 3 W-3.1 (May 14, 2026). |
| 240 | Yoga adapter test: positive level propagation assertion | Flutter / Test coverage | Yoga adapter happy-path tests assert level *negatively* (case 9 — fallback to 'beginner' when metadata absent) but no test asserts level *positively* (e.g. when `metadata.userLevels['yoga'] = 'intermediate'`, `result.level == 'intermediate'`). One-line addition to case 1: `expect(result.level, 'intermediate')`. Related architectural recommendation: establish a positive-assertion rule for fixture defaults across the project. Every default baked into a fixture should be asserted positively in at least one test, otherwise defaults become magic numbers that nothing locks. **Trigger:** Sprint 15 test-hygiene pass, paired with project-wide test convention update. Surfaced from S14-T6 `/review` on Commit 3 W-3.2 (May 14, 2026). |
| 241 | Yoga adapter test: parameterize `durationMinutes` for null/0/-1 | Flutter / Test coverage | Case 6 ("Throws on `duration_minutes <= 0`") tests one negative value (`durationMinutes: 0`). The adapter guard is `if (durationMin == null \|\| durationMin <= 0)` — three semantically distinct inputs collapse to one test. **Fix:** parameterize with `[null, 0, -1]` in a `for` loop, use `reason: 'durationMinutes=$bad should throw'` in the assertion so failure output identifies which branch fired. Same pattern applies to case 9 (`['', null]` — present-but-empty vs absent for `userLevels.yoga`). **Trigger:** Sprint 15 test-hygiene pass. Surfaced from S14-T6 `/review` on Commit 3 W-3.3 (May 14, 2026). |
| 242 | Verify invocation path of `recompute_user_pillar_level` PG function | Backend / DB hygiene | ARCHITECTURE.md generation (May 15, 2026) found that the `recompute_user_pillar_level` Postgres function exists in the database (introduced in S11-T4) but grep across `server/src/` finds no HTTP route or script that calls it. Three possibilities: (a) wired via DB trigger on `workout_sessions` or `sessions` inserts — safe; (b) wired via cron / scheduled job — safe; (c) truly orphaned, meaning pillar levels never recompute automatically — this would be a real bug. **Fix:** investigate by querying `pg_trigger` and `pg_proc` on the live DB. Resolution path: either confirm trigger/cron wiring and document it (likely in ARCHITECTURE.md §6 + a note on the function), or wire it explicitly from a session-complete handler. **Trigger:** Sprint 15 backend hygiene pass. Surfaced during ARCHITECTURE.md generation (May 15, 2026). |
| 243 | Smart-default "Start balanced session" CTA on home page below focus picker | UI / Onboarding | [ChatGPT review, May 15, 2026] ChatGPT recommended adding a one-tap "Start balanced session" CTA (`focus_slug=full_body`, `time_budget_min=30`, `entry_point=home`) on the home page to reduce friction for beginners who don't know which focus to pick. Initial assessment: placing this prominently would contradict Approach 5 (plan-first home model). However, a carefully-placed CTA *below* the focus picker — as a secondary action for users who explicitly opt out of focus-first planning — could complement Approach 5 without undermining it. **Defer until full onboarding flow lands (Sprint 19+)**, since better onboarding may solve the same beginner-friction problem without needing a magic button. Re-evaluate after onboarding ships with real user behavior data. Source: `DailyForge_Full_Code_UI_Review.md` §4.4. |
| 244 | Split remaining large files from ChatGPT's §5.1 monitoring list | Refactor / Code hygiene | [ChatGPT review, May 15, 2026] ChatGPT identified 12 files over 500 LOC that warrant monitoring or splitting. Sprints 15-17 address the top 6 (`suggestionEngine.js` in S15-T4; `routes/session.js`, `workout_session_provider.dart`, `multi_phase_session_page.dart` in S16; `half_pie_picker_sheet.dart`, `breathwork_player.dart`, `yoga_session_player.dart`, `strength_player.dart` in S17). Remaining 4: `migrate.js` (888 — split by version once staging DB exists), `progressService.js` (725 — query/helper split), `strength_page.dart` (633 — extract today-card logic), `seed-breathwork-techniques.js` (1417 — data file, structure separately if it grows). Address opportunistically as you touch them naturally. Source: `DailyForge_Full_Code_UI_Review.md` §5.1. |
| 245 | Convert `MultiPhaseSessionProvider` inheritance to config-object pattern | Refactor / Architecture | [ChatGPT review, May 15, 2026] ChatGPT noted the base class owns the real state machine and subclasses (`CrossPillarSessionProvider`, `StateFocusSessionProvider`) only override four values (`sessionShape`, `storageKey`, `useAutoAdvanceCountdown`, `phasesNoun`). ChatGPT itself rated this Low/Medium and recommended keeping inheritance until a third multi-phase shape appears, at which point convert to config-object pattern: `MultiPhaseSessionProvider(config: crossPillarConfig)` instead of subclassing. Trigger: when a third session shape is introduced. Source: `DailyForge_Full_Code_UI_Review.md` §3.7. |
| 246 | Replace `EmbeddablePlayer` boolean mode with sealed `PlayerMode` class | Refactor / Type safety | [ChatGPT review, May 15, 2026] ChatGPT noted the `bool isEmbedded` pattern creates boolean-mode branching that works but isn't safe. Recommended a sealed class hierarchy: `sealed class PlayerMode {} class StandalonePlayerMode extends PlayerMode {} class EmbeddedPlayerMode extends PlayerMode { final PhaseMetadata metadata; final void Function(PhaseResult) onComplete; }`. ChatGPT itself said "not urgent; keep current approach until players stabilise." Trigger: when player behavior diverges further between embedded and standalone modes. Source: `DailyForge_Full_Code_UI_Review.md` §3.8. |
| 247 | Professional review of red/yellow breathwork safety warnings | Safety / Launch gate | [ChatGPT review, May 15, 2026] User-acknowledged that breathwork safety warnings are self-curated based on research, not professionally reviewed. ChatGPT identified this as acceptable for invitation-only beta if warnings are conservative (S17-T2 handles the minimum bar — onboarding disclaimer, per-technique warnings, user acknowledgement for high-intensity, "stop if dizzy" copy). However, before public/paid launch, obtain professional medical review of red/yellow-level techniques and document contraindication sources. Trigger: before opening paid signups (Pro/Max tier). Source: `DailyForge_Full_Code_UI_Review.md` §8.4 and `ANSWERS-to-chatgpt-questions.md` Q4. |
| 248 | Route validation helper utilities (`parsePositiveInt`, `requireEnum`, etc.) | Backend / Code hygiene | [ChatGPT review, May 15, 2026] ChatGPT recommended extracting reusable route validation helpers: `parsePositiveInt(value, name)`, `parseOptionalPositiveInt(value, name)`, `requireString(value, name)`, `requireEnum(value, allowed, name)`. Would reduce repeated validation code across route files. S15-T7 (auth integer-id validation) addresses the most critical case; broader helper extraction is opportunistic. Address when touching multiple routes during a feature ticket. Source: `DailyForge_Full_Code_UI_Review.md` §10 Priority 4. |
| 249 | Rename legacy `cross_pillar_sessions_id_seq` sequence to match table | Schema / Cosmetic | Surfaced from S15-T1 schema snapshot (May 16, 2026). The S14-T5 table rename `cross_pillar_sessions` → `multi_phase_sessions` left the owned sequence with its pre-rename name; `public.cross_pillar_sessions_id_seq` is owned by `public.multi_phase_sessions.id`. Cosmetic only — Postgres doesn't auto-rename owned sequences, and inserts route via the OWNED-BY linkage, so no functional bug. **Fix:** `ALTER SEQUENCE cross_pillar_sessions_id_seq RENAME TO multi_phase_sessions_id_seq;` in `server/src/db/migrate.js`, idempotent-guarded. ~5 LOC. **Trigger:** next migration touch on `multi_phase_sessions` or an opportunistic SQL-hygiene pass. Priority: low. |
| 250 | Sentry IP / geography PII tightening | Privacy / Sentry config | Surfaced from S15-T2 device verification (May 16, 2026). The Flutter SDK is configured with `options.sendDefaultPii = false`, so the client never attaches email / IP / request headers. However, **server-side geo enrichment** at Sentry derives event geography (city, country) from the request IP that hit Sentry's ingest endpoint — visible on the event's user tab even with PII off at the SDK level. **Fix:** in Sentry Settings → Security & Privacy for the `dailyforge-flutter` project, enable **"Prevent Storing of IP Addresses"** + **"Scrub IP Addresses"**. Completes the PII-minimization story; not a leak today (IP isn't stored permanently, only used for geo) but worth closing the loop. **Trigger:** opportunistic, alongside any Sentry settings touch. Priority: low. |
| 251 | Auto-derive Sentry release from `package_info_plus` instead of `--dart-define=SENTRY_RELEASE` | Ergonomics / Build hygiene | Surfaced from S15-T2 build flow (May 16, 2026). Today the Sentry release name is either passed explicitly via `--dart-define=SENTRY_RELEASE=dailyforge-flutter@<v>+<b>` or left blank — when blank, Sentry derives it from binary metadata, which works but is opaque. **Fix:** add `package_info_plus` to `pubspec.yaml`, read `version` + `buildNumber` in `main.dart` before `SentryFlutter.init`, and set `options.release = 'dailyforge-flutter@$version+$buildNumber'` whenever the dart-define is empty. Removes a manual step from the release build command and keeps the release tag exactly in sync with the APK's pubspec.yaml `version:` field. ~10 LOC. **Trigger:** next Sentry config touch or a release-pipeline polish pass. Priority: low. |
| 252 | End-to-end Sentry pipeline verification before broader beta | Observability / Launch gate | [Claude Code /review R2, May 16, 2026] S15-T2 (Flutter client) and S15-T3 (Node server) each ship their own smoke verification — a deliberate-error event lands in each project's Sentry dashboard. What's missing is an end-to-end verification that ties the two halves together: trigger a real user-facing failure path in the Flutter app, confirm the client breadcrumb chain lands AND the server captures the corresponding 5xx with matching `user.id` on the same timeline. Today's smoke tests prove each half works in isolation — they don't prove the joined view actually reconstructs an incident. Worth doing once before broader beta opens up the event surface, while traffic is still low enough to spot mismatches by hand. **Trigger:** before broader invite beta opens (Sprint 17+ launch-gate checklist), or earlier if the first prod incident exposes a gap. Source: Claude Code `/review` on S15-T3 build, finding R2 (May 16, 2026). Priority: medium (launch-gate). |
| 253 | Smoke harness `test-suggestion-engine-t2.js` not idempotent across rapid repeat runs — strict byte-identical verification not achievable | DevEx / Test infrastructure | Surfaced from S15-T4 pre-flight (May 17, 2026). The engine smoke writes sentinel-tagged fixtures (T1, T5, T6, T7 blocks) to the staging DB. Cleanup hooks restore counts after each block, but if two smoke runs overlap (parallel invocation, or a fresh run before the previous one finished cleanup), they collide on the same sentinel emails / focus slugs and cascade into 28–41 false failures. Two sequential unchanged-code runs produce a 3596-line diff because of `ORDER BY random()` picks in test descriptions — making the spec's "byte-identical output" verification literally unachievable. S15-T4 worked around it by (a) running smoke strictly sequentially, (b) comparing only the `=== N pass, M fail ===` summary and the normalized FAIL signature set vs baseline. **Fix options:** (a) `SETSEED` the Postgres connection per run for deterministic randomness; (b) write a normalized smoke comparator helper (`scripts/lib/smoke-compare.mjs`) that strips PASS-line content IDs and JSON dumps, then diffs; (c) parallel-safe sentinel scoping (suffix each run's sentinels with a per-run UUID instead of shared strings). Without one of these, every future smoke-driven refactor will hit the same fragility. **Trigger:** before the next smoke-driven refactor (S16-T2 typed-error rewire is the obvious next consumer). Priority: medium. |
| 254 | Consolidate duplicate `mobility is not available from strength_tab` throw text after S16-T2 lands | Backend / Code hygiene | Surfaced from S15-T4 pre-flight throw inventory (May 17, 2026). The exact string `'mobility is not available from strength_tab — use yoga_tab or home'` is thrown from two places in the engine: `recipes/strength-only.js` (defensive guard inside `generateStrengthOnly`) and `suggestion-engine/index.js` (dispatch-level guard inside `generateSession`). Both are part of the route-substring contract (`routes/sessions.js:66-74` maps message substrings → stable error codes), so they had to stay in sync verbatim through S15-T4's extraction — which is why I left both intact. **Fix:** once S16-T2 replaces RangeError throws with `EngineContractError` carrying typed `code`, consolidate by either (a) defining a single error factory (`EngineContractError.mobilityFromStrengthTab()`) used by both throw sites, or (b) collapsing the dispatch guard since the recipe-level guard already covers the same case (T4 S3 defensive comment notes the dispatch is already in place). **Trigger:** S16-T2 (typed-error refactor) — this consolidation is the natural finishing touch after the typed-error rewire ships. Linked to S16-T2 per Prashob (May 17, 2026). Priority: low (closes a string-duplication on top of an already-in-flight refactor). |
| 255 | `fitMainCandidate` dead-code removal from `suggestion-engine/helpers.js` | Backend / Code hygiene | Surfaced from S15-T4 `/review` finding W-1 (May 17, 2026). `helpers.js:149` exports `fitMainCandidate(row, level, mainTarget)` but the function has **zero call sites** across the entire `server/` tree — verified by grep. It's a leftover from the pre-T3.5 retry-loop logic; T3.5's `state-focus.js:142` refactor replaced the retry pattern with a pre-filter (`mainPool.filter((r) => techniqueFitsBracket(...))`), orphaning `fitMainCandidate`. S15-T4 extraction carried it forward verbatim per the spec's no-cleanup rule. **Fix:** delete the function (~14 LOC) and the references in helpers.js's module-header listing (line 6) + state-focus.js's "Bracket helpers (... fitMainCandidate, etc.)" comment (line 11). **Bundle with S16-T2 typed-error refactor to amortize engine touch.** Priority: low. |
| 256 | `MOBILITY_MAIN_STYLES` dead-constant removal from `suggestion-engine/constants.js` | Backend / Code hygiene | Surfaced from S15-T4 `/review` finding W-2 (May 17, 2026). `constants.js:19` exports `MOBILITY_MAIN_STYLES = ['hatha', 'yin', 'vinyasa']` but the constant has **zero code references** — only mentioned in three comments (`yoga-only.js:178`, `cross-pillar.js:267`, `index.js:26`). S14-T6 Decision A switched mobility recipes from broad-style filters to single level-resolved style (via `resolveYogaSourceStyle(levels.yoga)`), orphaning the constant. S15-T4 extraction carried it forward verbatim. **Fix:** delete the export (1 line) and drop the constant's mention from the three comment sites (3 lines). Total ~4 LOC. **Bundle with S16-T2 typed-error refactor to amortize engine touch.** Priority: low. |
| 257 | Wire Sentry capture into `checkRecencyOverlap` DB-catch | Backend / Observability | Surfaced from S15-T4 `/review` finding S-1 (May 17, 2026). `suggestion-engine/recency.js:87-90` swallows DB errors via `console.error` + `return null`. The `return null` behavior is correct (per W1: recency is informational; a DB hiccup must not break session generation) — but `console.error` alone is invisible to ops. Now that S15-T3 has wired Sentry on the Node server, this catch should also `Sentry.captureException(err, { tags: { component: 'suggestion-engine/recency' } })` so silent DB hiccups are observable. **Fix:** import `Sentry` from the existing Node SDK setup, add one `captureException` call inside the catch block, preserve the `return null` behavior. ~5 LOC. **Bundle with S16-T2 typed-error refactor to amortize engine touch.** Priority: medium (observability gap on a load-bearing engine path). |
| 258 | Whitelist trusted fields when assigning `req.user` post-jwt.verify | Backend / Security hardening | [Claude Code /review, 2026-05-17] S15-T7's `authenticate` middleware does `req.user = decoded` after validating `decoded.id`, attaching the entire JWT payload. Today only `id` and `email` are issued (`server/src/routes/auth.js:44` + `:72`), so nothing untrusted exists in the payload — but if JWT issuance ever adds a `role` / `admin` / `permissions` claim that the server later reads, anyone with a leaked `JWT_SECRET` (or even a misconfigured test token that slips into prod) could elevate privileges by signing their own. **Fix:** narrow the assignment to `req.user = { id: decoded.id, email: decoded.email }` (whitelist). When new trusted claims are added, extend the whitelist explicitly. Pairs with FS #248 (general validation helpers). **Trigger:** Sprint 17+ security hygiene sweep, OR before any new claim is added to issuance. Priority: low (no current attack surface; pure defense-in-depth). Surfaced from S15-T7 `/review` finding S1 (May 18, 2026). |
| 259 | Verify or re-extract `smoke-fixtures.mjs` shared helper | Tooling / Tracker integrity | [Claude Code S15-T6 pre-flight, May 17, 2026] FS #168 was treated as shipped at S13-T7 close, but S15-T6 pre-flight found `server/scripts/lib/` contains only `prod-guard.mjs` — no `smoke-fixtures.mjs`. The sentinel pattern is still inline inside `server/scripts/test-suggestion-engine-t2.js` (20 occurrences) and in `server/scripts/t1-curl-roundtrip.mjs`. The S15-T6 spec originally assumed a `db-sentinel.js` test helper could re-export from this file; the assumption was dropped in S15-T6-AMENDMENT-1. Resolution paths: (a) audit S13-T7 commits — was the extraction renamed or never landed? (b) extract the pattern now as its own ticket, or (c) close FS #168 with "not building, sentinel pattern stays inline" and update the doc accordingly. **Trigger:** before any future ticket assumes the helper exists, or as part of S16 engine-test work which may want sentinel cleanup. Priority: low (smoke harness works today). Source: S15-T6 pre-flight verification, May 17, 2026. **[Renumbered at sprint-15-close merge, May 18, 2026]** Originally drafted as FS #253 on main during post-T6 hygiene chore commit; renumbered to #259 at sprint-15-close merge because the sprint-chain branch had already filled #249-258 with S15-T1 through S15-T7 review-surfaced items. No content change. |
| 260 | N+1 query on `GET /api/home/stats` (10 pg-pool.connect calls per request) | Backend / Performance | Surfaced from Sentry `dailyforge-node` N+1 detector classification during S16-T2 device verification (May 19, 2026). **Pre-existing, NOT introduced by S16-T2** — the detector just classified an existing pattern when an unrelated trace landed in Sentry. **Severity:** medium (performance, observable to user as latency on home page). **Investigation:** review `server/src/services/home_service.js` or `server/src/routes/home.js` — likely an inner loop calling a per-row query (e.g. iterating sessions and querying breathwork_sessions per id, or per-pillar streak queries). **Likely fix:** consolidate to a single batched query (`WHERE id IN (...)`) or JOIN across the relevant tables, mirroring the `v_completed_sessions` direction FS #212 is already pointing at. **Trigger:** before public beta — home page latency directly shapes first-impression UX. Priority: medium. Source: Sentry N+1 detector + S16-T2 device verification, May 19, 2026. |

---

## ❌ Cut (2 items)

| # | Feature | Category | Reason |
|---|---------|----------|--------|
| 3 | Coach platform | Social | Not relevant for personal use app |
| 6 | Desktop responsive layout | Platform | Mobile-first app, desktop not priority |

---

## ✅ Already Done (6 items)

| # | Feature | Completed In |
|---|---------|--------------|
| 19 | Workout tab home screen redesign | S5-T5 |
| 47 | Custom workout builder | S6-T3 |
| 48 | Empty workout start | S6-T1 |
| 50 | Strength exercise browser | S6-T1 |
| 51 | Strength page redesign | S6-T1 |
| 58 | Extract SaveRoutineModal pattern | S6-T3 |

---

## How This File Works

- PM (Claude.ai) adds entries during planning sessions
- Features move OUT of this file and INTO the sprint backlog when prioritized
- Each entry gets a date so we can see how long ideas have been waiting
- "Source" tracks where the idea came from (competitor research, user feedback, internal planning)
- ⚠️ APPROACH 5 IMPACT notes flag items affected by the Apr 26, 2026 home-page-model pivot. See `Trackers/PRE_SPRINT_11_PLANNING.md` for full strategic decisions.
